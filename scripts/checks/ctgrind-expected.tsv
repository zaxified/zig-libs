# Expected values for `scripts/checks/ctgrind.sh --check`. One row per CLAIM row of
# the table — i.e. per (module, optimize mode, harness target) measured with
# the secret TAINTED and the binary built WITH `-fvalgrind`.
#
# The control rows (untainted) and trap rows (no `-fvalgrind`) are not listed:
# the script requires 0 from every one of them unconditionally, since a
# non-zero there means the measurement itself is broken, never that the module
# changed.
#
# Deliberately NOT a diff of exact totals. `total` includes the harness's own
# non-constant-time hex formatter and whatever std internals it reaches, so it
# moves with a zig or valgrind upgrade for reasons that say nothing about the
# module — and a check that goes red for the wrong reason gets muted, which is
# worse than no check. `total_min` therefore only asserts that the propagation
# witness fired at all (a zero total would mean the taint never arrived, which
# would make every in-file zero meaningless).
#
# `in_file` accepts an exact count or a `>=N` / `<=N` bound:
#   exact  — the number a SPEC.md states as a fact about the module. Every
#            claimed zero is exact, and so are the small non-zeros that are
#            documented line by line (e.g. ed448's three `Fe.invert`
#            validations). ecvrf's `prove` used to be the other example, at
#            three try-and-increment branches on the PUBLIC H; since
#            4db1e627 the harness declassifies the public key and the row is
#            an exact 0.
#   bound  — counts a compiler or std change may legitimately move, where only
#            the direction is load-bearing: the ReleaseSafe
#            overflow-check flood that is chachapoly's positive control, and
#            std's `rejectIdentity` context that ct25519's negative control
#            needs only to EXIST.
#
# `out_sha` — the SEVENTH column, added 2026-09-08. A digest of the VALUE lines
# the harness itself printed on this row's claim run.
#
# ⛔ Why. The ct25519 audit (C1) mutated the HARNESS, not the module: the branch
# stopped calling `mulRistrettoBase` and assigned the secret straight to the
# output. The table came out identical to the baseline — 2 / 0 / 2 / 0 — and
# `--check` was GREEN. Every row claiming "in-file 0" has that hole, because a
# harness that never reaches the module also produces zero in-file contexts:
# "no secret-dependent branch" and "the module was not called" are the SAME
# measurement. That is 8 rows across 6 modules.
#
# What separates them is the value the harness prints, which every harness
# already did (`pk=`, `sig=`, `x=`, `create[N]=`, `cmov=`, `r=`/`s=`) — nothing
# needed a new print, only a pin. Verified on the deployed bypass: the counts
# stay 2/0/2/0 and pass, and ONLY this column fails.
#
# The two pins are complementary and neither subsumes the other. The source
# digest sees an edit to the analysed module; the output digest sees the harness
# failing to reach it. The bypass lives in the harness, which is not in any
# row's pattern, so the source digest does not move for it.
#
# Rewritten by `--update-outputs`, which RUNS the measurement (the value cannot
# come from anywhere else) — a separate verb from `--update-digests` for the
# same reason: re-pinning must be typed on purpose.
#
# Equal digests across rows are expected in a few places, all legitimate:
# - chachapoly's two modes compute the same tag.
# - ct25519's `ct25519`, `std` and `ladder` compute the same ristretto point
#   by different routes.
# - ct25519's `comb` and `ladderbase` compute the same Edwards point.
# - aescbc's two strippers, and sealedbox's two parsers, return the same value.
# Each row still reads its OWN log
# (`log_<module>_<mode>_<vg>_<target>_<taint>.txt`), so no row can be scored on
# another's output.
#
# `src_sha` — the SIXTH column, added 2026-09-08. A 16-hex-digit sha256 over
# the module's OWN sources named by this row's `PATTERN`, concatenated in
# sorted order.
#
# ⛔ Why a source pin and not a location list. The k256 audit (G1) showed the
# in-file COUNT cannot see a leak that REPLACES an expected one, and proposed
# pinning `file:line` sets instead. Measured before building it: that does not
# work either. The reproduction substitutes `ecdsa_recover.zig:136` IN PLACE,
# so the file's numbering is unchanged and the stack line set is identical —
# and so is the error count (139 errors from 15 contexts, both runs). The two
# memcheck logs differ only in the pid. Nothing derived from the measurement
# separates them.
#
# So the pin is on the artefact: if the analysed sources change, the row goes
# red until someone re-reads the contexts and runs `--update-digests`. Coarse
# on purpose — a comment change trips it — because "the code that was analysed
# is the code that ships" is what the counts silently assumed.
#
# Verified on the deployed defect: with the substitution applied, every count
# check still passes (15/10/5/0, the baseline exactly) and ONLY the digest
# fails. Unmodified tree stays green. The defect deployed into each of the 43
# files these patterns name turns its own row red — no blind spots.
#
# `NO-OWN-SRC` means the pattern names no file of this module (ct25519/std
# points at std's sources). Such a row has no artefact pin and `--check` says
# so on every run rather than comparing two placeholders and passing.
#
# A NON-ZERO in_file is NOT automatically "fine". Every non-zero below is an
# accounted-for validation, itemised line by line in the owning module's
# SPEC.md — but the convention when one is NOT is to record it here anyway,
# marked DEFECT in the comment above the row. Recording a defect is deliberate:
# an unrecorded one is invisible, and a row that goes red when it is FIXED is
# how the fix gets noticed. That is exactly how the two montint rows below went
# from 7 and 5 to 0. Nobody should read a green `--check` as "every
# constant-time claim in this repo holds": it means "the measurements still say
# what they said when they were taken".
#
# Measured 2026-08-11 (chachapoly…ed448) and 2026-08-13 (k256, montint),
# zig 0.16.0, valgrind 3.26.0, x86_64 (i7-7920HQ), `poly1305.lanes = 4`. Change
# a number here only together with the module's SPEC.md table, and only after
# re-running the harness.
#
# module	mode	target	total_min	in_file
# bolt8 — the module was outside this table until 2026-09-08 and its ledger
# recorded the constant-time verdict as "PASS (inherited)": read, never
# instrumented, while it holds the node's long-term static private key, the
# ephemeral keys, `temp_k*` and both transport keys (audit F10).
#
# ⚠ EVERY NON-ZERO BELOW IS NAMED, and none of them is a leak. In order:
#   dh 1        `q.rejectIdentity()` at the end of k256's constant-time ladder
#               (group.zig:278) -- a branch on "the product was the identity",
#               reachable only for k == 0 mod n. Same class k256's own table
#               already accounts for.
#   keygen 3    the invalid-secret rejections of `generateDeterministic`:
#               scalar canonicality (common.zig:75 via scalar.zig:87),
#               `if (d.isZero())` in bolt8's own dh.zig:80, and the same
#               `rejectIdentity` inside `combMulBaseWithTable` (group.zig:347).
#               All three reject a key that would be discarded anyway.
#   act3 6      2x std's `if (!verify)` tag check (chacha20.zig:713) -- branching
#               on the ANSWER is the API, and the comparison producing it is
#               std's constant-time one; plus 4 from decompressing the peer's
#               static key (group.zig:116/123/124, field.zig:162, sqrt at
#               field.zig:306) and one ECDH `mul`. `rs` is the peer's PUBLIC key,
#               but it arrives by decryption under a tainted key, so memcheck
#               cannot know that -- an artefact of the taint model, stated
#               rather than patched around.
#   transport 3 2x the same tag check, and `if (c.len != out.len + 16)` in
#               bolt8's transport.zig:149. The length came from `recvLength`,
#               i.e. from a decryption under a tainted key; a BOLT#8 message
#               length is a public property of the frame.
#
# `act1` has no row on purpose: the initiator's Act One never touches the static
# key, so its zero would say nothing about constant time. See the harness.
bolt8	ReleaseFast	dh	1	1	133f64b49f95282f	0147038b2745ef44
bolt8	ReleaseFast	keygen	1	3	133f64b49f95282f	d7a8d7540234ad3e
bolt8	ReleaseFast	act3	1	6	2d54eabc5fe940df	f60e20889f25f0aa
bolt8	ReleaseFast	transport	1	3	cb5d5d200f748105	859416d386f56f8a
bn254	ReleaseFast	field	1	1	27af92a0c130f0a6	831aa25b5c3d2f05
# bn254 scalarmul 5 -> 2 (re-pin 2026-09-15): `3cda42bf`/`b80e3cce` made
# `Fr.toBytes` reduce with bn254's own CIOS behind a `blackBox` barrier instead
# of `std.crypto.ff`'s `fromMontgomery`; exactly the three contexts attributed
# to std's ff went (9 -> 6 total), output byte-identical.
bn254	ReleaseFast	scalarmul	1	2	48c2ef1eb2373d6b	1bba83824af4d763
chachapoly	ReleaseFast	poly1305	1	0	f3f338165286691b	ff3ba8f00b851bdf
chachapoly	ReleaseSafe	poly1305	1	>=1	f3f338165286691b	ff3ba8f00b851bdf
# The AEAD's own claim (added 2026-09-02). SPEC.md and root.zig state that the
# tag is compared in constant time, and until this row existed the module was
# measured with the `poly1305` target ALONE -- the harness never touched
# `root.zig`, so swapping `timing_safe.eql` for `std.mem.eql` left the whole
# suite green.
#
# ⚠ THE EXPECTED COUNT IS NOT ZERO, AND THE FOUR ARE NAMED. `in` for this
# target is `root.zig` PLUS std's `chacha20.zig`/`poly1305.zig`, because the
# module delegates every AEAD call at or below `aead_delegate_max` to std --
# their property is ours for those lengths, and attributing the contexts to
# std would be the same evasion as widening a pattern to make a count go away.
# At ReleaseFast the four are the ONE branch an AEAD cannot avoid, twice on
# each side of the delegation threshold: `if (!timing_safe.eql(computed, tag))`
# in `root.zig` (2) and the identical `if (!verify)` in std's `chacha20.zig`
# (2). Branching on the ANSWER is the API; the comparison that produced it is
# what has to be constant-time, and that is what the two `timing_safe.eql`
# calls are. A fifth context would mean a new branch on secret data.
# ReleaseSafe is `>=1` for the same reason the poly1305 row is: the
# lane-parallel MAC's checked arithmetic is reached through the AEAD too.
#
# ⛔ THE TWO DEBUG ROWS WERE REMOVED 2026-09-08, and the cause is now known.
# They were recorded here as KNOWN RED on 2026-09-02 -- valgrind printed the
# MAC's frames with no file:line at all
# (`poly1305.Generic(4).wide (in …/ctgrind-chachapoly)`), so no pattern could
# attribute them. On 2026-09-08 the SAME expectation passed on the same tree.
# The verdict was moving with the build, not with the module.
#
# Root cause: Zig 0.16 compiles Debug with the self-hosted x86_64 backend
# (the only mode where it is the default) and valgrind's DWARF reader cannot
# parse the `.debug_line` it emits -- 35 159 warnings on a ten-line program
# with no inline asm and no `std.valgrind` call, 0 for the same program built
# `-fllvm`, and the release modes break identically when forced `-fno-llvm`
# (48 395 / 37 369). Measured effect on attribution, same harness and target:
# ReleaseFast 36/36 frames carry `(file:line)`, ReleaseSafe 2054/2054, Debug
# 904/2130 -- and 51 of 60 resolved Debug frames carry the WRONG line.
#
# Not worked around with `-fllvm`, because the rows bought nothing: SPEC.md
# calls ReleaseSafe and Debug ONE positive control (210 and 281 contexts inside
# `poly1305.zig`, the same checked operators for the same reason), nothing here
# is consumed in Debug, and CONVENTIONS §7.1 narrowed the Debug CI lane to
# compile-only in 2026-08-15 on the same kind of measurement. See
# `scripts/checks/ctgrind.sh` § MODES.
chachapoly	ReleaseFast	aead	1	4	b08b7e78d8c87360	870a36cbebbc4588
chachapoly	ReleaseSafe	aead	1	>=1	b08b7e78d8c87360	870a36cbebbc4588
# oscore — added 2026-09-08. SPEC.md § "Threat model / limits" claims the key
# material is routed ONLY through std's constant-time HMAC/AES-CCM, with no
# comparison or branch on key bytes in this module's own code; until today that
# sentence had no measurement behind it (audit F8).
#
# ⚠ THE TWO ZEROS ARE ZEROS WITH A WITNESS. An earlier probe reported `derive`
# and `protect` as 0 in-file out of a total of 0, which is not a result: a zero
# total is what a harness that never reaches the module reports too. This
# harness prints the derived keys and the ciphertext, so the totals are 4 and 2
# -- taint demonstrably arrived at the formatter -- and the in-file zero means
# "no branch found".
#
# `unprotect` 1 is std's `if (!valid)` at aes_ccm.zig:152, AFTER the
# `crypto.timing_safe.eql` two lines above it. Branching on the answer is the
# API; the comparison that produced it is the constant-time one. It is here and
# not in the other two because only `unprotect` verifies a tag.
oscore	ReleaseFast	derive	1	0	71a979efe6e701d2	2f7c17dad79da56d
oscore	ReleaseFast	protect	1	0	71a979efe6e701d2	6eac6a90e3479ab8
oscore	ReleaseFast	unprotect	1	1	71a979efe6e701d2	ed85db89e3d7e9fd
# ct25519 — columns below are `total_min in_file`, NOT "in_file total_min".
# Row 1's `1 0` is `total_min=1, in_file=0`: ct25519's OWN code has zero
# expected contexts (the `total_min=1` is only the harness's print, a
# propagation witness that the taint reached output at all). The `bound`
# comment earlier in this file ("std's `rejectIdentity` context that
# ct25519's negative control needs only to EXIST") is about row 2
# (`ct25519 ReleaseFast std 1 >=1`), NOT this one — row 2 is std's own
# source, hence `NO-OWN-SRC` in the src_sha column. Read `1 0` on row 1 as
# "one witness context, zero in-file", never as "one expected context in
# ct25519's own code" — that reading would describe the fragile
# single-context pin this module deliberately does not have.
ct25519	ReleaseFast	ct25519	1	0	3af4421d3df4cd83	4e5e08fafcda58f8
ct25519	ReleaseFast	std	1	>=1	NO-OWN-SRC	4e5e08fafcda58f8
# ct25519 targets added 2026-09-16, first pinned 2026-09-15 (A1 ct25519 C3/C4,
# bulletproofs B9). All four claim zero in-file contexts, 2 witness each
# (`printHex` of the result). Their positive controls are recorded in
# A1/ct25519.md: `pc[slot]` for `pcSelect` puts `ladder`/`ladderbase` at 6/6
# on `root.zig:186`; a secret-digit `if` in the MSM puts `msm` at 2/2.
#   comb        `mulBase`, the fixed-base comb (C3).
#   ladderbase  `mul(basePoint, s)`, the window ladder over the comptime table.
#   ladder      `mulRistretto` over a point decoded at run time (C4). Prints
#               the same point as the `ct25519` row, so its output pin equals
#               that row's.
#   msm         `mulMultiRistretto` over 9 terms (B9).
ct25519	ReleaseFast	comb	1	0	3af4421d3df4cd83	60c9dcf57e8f3e1f
ct25519	ReleaseFast	ladderbase	1	0	3af4421d3df4cd83	60c9dcf57e8f3e1f
ct25519	ReleaseFast	ladder	1	0	3af4421d3df4cd83	4e5e08fafcda58f8
ct25519	ReleaseFast	msm	1	0	3af4421d3df4cd83	55f7fb062759921f
decaf448	ReleaseFast	scalarmul	1	0	e664aa6778fea0b1	77a1bd1038ca1053
# ecvrf prove 3 -> 0 (4db1e627): the three contexts were try-and-increment
# branches on H, which is derived from the PUBLIC key. The harness now
# declassifies Y, so what remains is a claim about the secret alone.
# `verify` is DOCUMENTARY. Verify holds no secret: the target taints the public
# `alpha` only to list its input-dependent branches, and a bound is all it
# pins. It does NOT catch E15's `timing_safe.eql` -> `mem.eql` mutation
# (measured, same count). Read no constant-time claim into this row.
ecvrf	ReleaseFast	prove	1	0	3d03912e3085ecd6	f676793e0800a787
ecvrf	ReleaseFast	verify	1	>=1	3d03912e3085ecd6	087c8ff4d336d9b1
ed448	ReleaseFast	full	1	3	dbefc4ae560d8288	5853bb35135afb85
ed448	ReleaseFast	ladder	1	0	dc6585ac248e91ee	43db8426e0d6c6e7
# k256 — the field layer is clean; every group/sign/ecdsa context is a
# `rejectIdentity`, an input/output validation, or (in `ecdsa` alone) the RFC
# 6979 §3.2 nonce-retry rejection, itemised line by line in
# `modules/k256/SPEC.md` and documented at the source in `ecdsa_recover.zig`.
# `ecdsa` was added 2026-08-13: `ecdsa_recover.sign` is a shipped secret path
# with an in-repo consumer (`lninvoice`) and had no target at all.
# ⛔⛔ hqc — THESE FOUR ROWS ARE A RECORDED DEFECT, not a clean claim, and they
# are the first ones in this file that are. The convention above says an
# unaccounted non-zero is recorded here anyway and marked DEFECT, because an
# unrecorded defect is invisible and a row that goes red when it is FIXED is
# how the fix gets noticed. This is that case — and on 2026-09-09 it did
# exactly that: the counts below were 52 / 26 / 33 and are now 14 / 4 / 6.
#
# The module's own docs said it has no secret-dependent branches because it
# structurally matches the reference. It does match — and the compiler undoes
# it. `writeSupportToVector` is a masked select with no `if` in the source;
# LLVM recognised the identity and rewrote it back into a branch that loads
# `bit_tab[k]` only on the taken path. Adjudicated in the disassembly as real
# `je`/`jne`/`jb`, not the known `cmov` false positive. That is now stopped by
# an `asm volatile ("" : "+r" (mask))` barrier in the inner loop — an opaque
# identity the compiler cannot see through, so it can no longer prove the
# select. THE BARRIER IS LOAD-BEARING: deleting it puts 38 contexts back, and
# these rows are what says so.
#
# WHAT THE FIX COST, measured before it was applied (min of 200 calls,
# ReleaseFast -Dcpu=native, three alternating runs per arm; the spread inside
# each arm was ≤1%, so the gap is far outside the noise):
#
#             without barrier   with barrier    delta
#   keypair       141 175 ns      150 978 ns    +6.9%
#   encaps        274 923 ns      298 035 ns    +8.4%
#   decaps        442 039 ns      469 334 ns    +6.2%
#
#   Re-derive with `modules/hqc/src/bench.zig` (opt in via HQC_BENCH=1); it was
#   written for this measurement. The trade taken: 38 secret-dependent branch
#   contexts out of the module's hottest secret path, for ~7% on a PQ KEM.
#
# Attribution, ReleaseFast, decaps (14) — `prng.zig:289`, which was 38 of the
# old 52, is GONE; everything below is what it always was:
#   gf256.zig:114   4   `if (s >= 255) s -= 255` — log-table reduction.
#   gf256.zig:113   3   `if (a == 0 or b == 0)` — GF(256) multiply short-circuit.
#   prng.zig:268    3   `if (found) support[idx] = …` — duplicate fixup.
#   reedsolomon     2   inlined, no line info (computeElp and encode).
#   prng.zig:216    1   `if (val < threshold) break` — rejection sampling.
#   prng.zig:223    1   `if (s == val) dup = true` — duplicate detection.
#
#   keygen (4): prng.zig:216 ×2, prng.zig:223 ×2 — the rejection loop only.
#   encaps (6): prng.zig:268 ×3, gf256.zig:114 ×2, reedsolomon ×1.
#
# `sampler` is 2, not 40, ON PURPOSE: that target drives the fixed-weight
# rejection sampler only (prng.zig:216 and :223) and never reaches the scatter,
# which runs later when the support is turned into a bit vector. It isolates
# the loop so a change there is attributable; it is not a smaller measurement
# of the same thing. It is unchanged by the barrier, which is the expected
# result and a small check that the drop elsewhere is the scatter and not a
# measurement artefact.
#
# ⚠ The count is STILL WORSE in the mode a cautious consumer deploys, and the
# barrier does not fix that: measured on decaps, ReleaseFast 14, **ReleaseSafe
# 92** (was 130). The remaining ~78 are the overflow checks in
# reedmuller.decodeSymbol becoming branches on secret-derived data — a
# different defect in the same module, untouched by this fix. Only ReleaseFast
# is pinned; a second mode doubles the rows for a claim of the same shape, and
# Debug cannot be measured at all.
#
# ⚠ HONEST BOUND: memcheck says "Conditional jump OR MOVE". These were
# adjudicated as real jumps. Whether a key is practically extractable from them
# was NOT measured; that is different and much more expensive work. The claim
# is "the module's stated property does not hold and nothing was testing it",
# not "the key is out".
# hqc, 2026-09-09. decaps 14 -> 2, encaps 6 -> 0. FOUR fixes, all measured:
#   gf256.mul/inverse   dropped the exp/log tables -- the only secret-INDEXED
#                       table loads verified anywhere in this repo (+7.3% decaps)
#   sampleFixedWeightBiased   duplicate fixup blends under a mask
#   maskNonzero               laundered; LLVM had turned the branch-free
#                             `x != 0` trick back into a branch
#   Berlekamp-Massey mask12   same, one barrier covers the whole update
# The last three cost nothing measurable; the 7.3% is the algorithm swap alone.
#
# ⭐ Everything left, in EVERY target, is `sampleFixedWeightRejection`
# (prng.zig:216 the rejection loop, :223 the accept decision). Spec v5.0.0 §3.5
# REQUIRES that sampler for keygen's x/y, and decaps re-derives y from the seed
# on every call because the spec fixes a seed-sized dkKEM. ⛔ Do NOT mask :223 to
# make this number smaller: the leak is the ENCLOSING loop's trip count, so the
# context would go and the timing signal would stay -- a prettier measurement of
# the same defect. See SPEC.md.
# ⭐ encaps' 0 means something only because the row carries an output pin.
hqc	ReleaseFast	decaps	1	2	1b33fe8f22ee9b7e	91caa495ae54247e
hqc	ReleaseFast	keygen	1	4	1b33fe8f22ee9b7e	a20c02ff5f2e81ee
hqc	ReleaseFast	encaps	1	0	1b33fe8f22ee9b7e	e91a390cf03fd6ad
hqc	ReleaseFast	sampler	1	2	8eeeec94620e623e	b93fa60c89578576
k256	ReleaseFast	field	1	0	9cea496ddcd0b4a7	4f13cccddea81376
# k256, re-pin 2026-09-15, after F5 (1ce3087e: signed-window variable-base
# mul) and the re-audit fixes in ff305abc.
#   mul 2 -> 1   7/1/6/0. The one context is `rejectIdentity` at
#                group.zig:347. ⚠ "1" is an inlining outcome, not a module
#                property: without the `noinline mulInner` it reads 2. The
#                OUTPUT pin moved too, and that is R2 -- the target now
#                multiplies 2G decoded at run time instead of the comptime
#                base point, which let LLVM fold `mul`'s per-call table (not
#                the ECDH binary).
#   ecdsa 10 -> 11   16/11/5/0 (G7 19a05328, `sign` handles R.x >= n).
k256	ReleaseFast	mul	1	1	9fcd738bb165a0e6	2e85d4bddb851304
k256	ReleaseFast	comb	1	1	9fcd738bb165a0e6	2ac21fe395f35487
k256	ReleaseFast	sign	1	11	854fbd1bdf3d7050	bb06c2ed85209b8d
k256	ReleaseFast	ecdsa	1	11	de35a6b6fdf0588c	8ff4812d21149442
# montint — all three dispatch sizes clean, no residual. `small` (L=4) and
# `portable` (L=16) were DEFECT rows at 7 and 5 when the harness was added:
# ReleaseFast lowered `condSubTop`'s masked select, and `sub`'s masked add-back,
# to a `test`/`jne` on the secret-derived borrow — the Montgomery
# final-subtraction leak, inside `powMont`. Fixed 2026-08-13 by laundering both
# masks through montint's `blackBox` barrier; re-measured 0 and 0. Note the
# structural rewrite alone did NOT fix it (7 → 7, 5 → 5) — see
# `modules/montint/SPEC.md` § "Constant-time contract" for that table. Any
# non-zero appearing here again is a regression, not a new validation.
montint	ReleaseFast	small	1	0	045eb3a8ac23bebb	4e9f8c0b1ac8efe3
montint	ReleaseFast	portable	1	0	045eb3a8ac23bebb	5c44fe7ce913f8f6
montint	ReleaseFast	asmcore	1	0	045eb3a8ac23bebb	51c0595ca47bd7cc
p256	ReleaseFast	comb	1	1	aefd5530fd6443f0	9d70061e0f0133d9
p256	ReleaseFast	sign	1	<=9	a9c8d581ee749255	af4baed5c5134f3f
rsa	ReleaseFast	crt	1	<=213	cd9d282d526ba2de	d4057b00f9df7b56
rsa	ReleaseFast	noncrt	1	2	cd9d282d526ba2de	d4057b00f9df7b56
slhdsa	ReleaseFast	seed	1	4	6848022d967ef676	e0fdde7f5c62a2d6
slhdsa	ReleaseFast	prf	1	8	6848022d967ef676	e0fdde7f5c62a2d6
falcon	ReleaseFast	sign	1	<=133	d6b62d587b79541c	3f5a6dbb61a76d9d
bls12_381	ReleaseFast	field	1	1	397efcbf4800ff52	c1e8a3c184eada53
bls12_381	ReleaseFast	g1_scalarmul	1	5	899d68d0a1824f0e	b53b0d7808e48c1c
bls12_381	ReleaseFast	g2_scalarmul	1	8	c58d13bebb766407	7f4e15a241002b3f
# Re-pin 2026-09-15. Every moved bound below was diffed context-by-context
# against a --stacks run of the tree at 0c0ef527:
#   bip340 sign 79 -> 80   The F5 seam (f3c75612) moved sign's body into
#                signImpl/computeUnverified, keeping every old context under
#                the new frame names, plus one: `if (!verify(...))` at
#                root.zig:343, the branch on the self-check's verdict for a
#                signature about to be published.
#   blindrsa blind 477 -> 493   +18 contexts (and 11 fewer memmove contexts),
#                every one std.math.big inside `bigModInverse`'s Euclid over
#                the MASKED operand x·u mod n. B16 (088d096e) makes `blind`
#                draw its PSS salt from `random` first. That shifts the
#                untainted ChaCha stream the mask `u` comes from, so the
#                variable-time trace length moved with the mask. Same class
#                as before, and so is the output pin: the new salt shows in
#                the blinded message.
#   threshold_ecdsa share 127 -> 273   The 127 was stale long before F5: the
#                tree before 9cc2c425 already read 272. The +1 is
#                `paillier.decrypt` (A5 class), which F5's wider plaintext
#                reaches (A1 threshold_ecdsa, re-audit 2026-09-15).
bip340	ReleaseFast	sign	1	<=80	aa70b949dabd149a	0ce3274dc49157be
blindrsa	ReleaseFast	blind	1	<=493	ec3c73bfc9e557df	4352055809b3f3c7
blindrsa	ReleaseFast	sign	1	<=218	ec3c73bfc9e557df	4d5e3740a158032d
# A1 threshold_ecdsa R2 fix (2026-09-15, fixwt/a): `zkproofs.mulAddBytes`'s
# carry-propagation tail no longer exits early on `carry != 0` (which compiled
# to `cmp $0x100`/`jb` over the tainted running sum, zkproofs.zig, was :356);
# it now always walks every remaining byte down to `out[0]` (`pos`-driven, and
# `pos` is a function of public `e.len`/`x.len`/`i` only). Differential test
# (`audit R2 ... bit-exact against the old early-exit shape`) proves the two
# shapes are bit-identical; the harness's own out_sha below is unchanged
# across the fix, which is the same claim from the signing side. Positive
# control: temporarily restoring `while (carry != 0)` put share/nonce/
# betaprime back to EXACTLY their pre-fix counts (273/390/321), same command,
# same tree otherwise. `grep -oE 'zkproofs\.zig:[0-9]+'` over the post-fix
# `--stacks` log has no hit in [300,399) (the mulAddBytes/mulAddOwned/
# mulBytes region) for any of the three targets.
#
# Both rows below carried R2's contexts (8 for share, 12 for nonce, per the
# original R2 finding's per-target breakdown) inside bounds pinned before R2
# was known (2026-09-09). Measured post-fix: share 269 total / 267 in-file
# (was 273, -6) / 2 witness / 0 unattr; nonce 382 total / 378 in-file (was
# 390, -12) / 4 witness / 0 unattr. ⚠ share's drop is -6, not -8 — not
# independently re-derived here (only betaprime's 9 non-shared R2 locations
# were disassembled one by one); the direction and the disassembly/`--stacks`
# evidence above are what this pin rests on, not the exact arithmetic on this
# row.
threshold_ecdsa	ReleaseFast	share	1	<=267	e932a70c2bfffc2e	4a6777ac538b5e90
threshold_ecdsa	ReleaseFast	nonce	1	<=378	e932a70c2bfffc2e	31f7e323b84fd8a7
# ✅ threshold_ecdsa betaprime — R2 FIXED 2026-09-15 (was DEFECT, RED ON
# PURPOSE, see git history of this comment for the pre-fix analysis).
# Added 2026-09-15 for R1: nothing tainted Bob's 160-byte MtA blind β' before.
#
# Measured on the R1 tree (pre-R2, unmutated otherwise): 325 total / 321
# in-file / 4 witness / 0 unattributed, control 0, trap 0, `draws_160b=4`.
#
# Positive control (R1): a `testb`/`je` on `β'[100]` added exactly 2 contexts,
# on the inserted line, one per caller.
#
# Of the 321, nine locations reach β' and no other row. Every one was
# disassembled, and none is a new leak class:
#   - `std.mem.order`'s early exit in `sampleBetaPrime` (4): reveals only how
#     far β' matches q⁵'s 0xff prefix. Alice reads those bytes off α' anyway,
#     and β mod q is independent of them.
#   - The `Fe.fromBytes(N², β')` canonicality verdict (1).
#   - std `crypto.ff` `montgomeryMul`/`add` final subtraction (13): `ct.eql`
#     lowered to `jne`. This is the std-internal class already inside the
#     share/nonce and paillier bounds.
#   - Alice's verification of the PUBLIC t/t1 (2): over-taint from simulating
#     both parties in one process.
#
# R2 (2026-09-15): the remaining 8 were `zkproofs.mulAddBytes`'s carry tail
# (zkproofs.zig, was :356) branching on secret-derived partial sums of
# `e·x + mask` — 321 - 8 = 313, matching the bound this row had carried since
# R1 as the anticipated post-fix count. Measured post-fix (fixwt/a,
# `mulAddBytes` now fixed-length): 317 total / 313 in-file / 4 witness /
# 0 unattributed, control 0, trap 0 — in-file matches the anticipated number
# exactly. Positive control (R2): temporarily restoring the early exit put
# this row back to EXACTLY 321 in-file, on the same tree, same command.
threshold_ecdsa	ReleaseFast	betaprime	1	<=313	e932a70c2bfffc2e	0d9a7f3232367fc7
# Output cannot repeat: `prove` draws alpha, s_l/s_r, rho and tau1/tau2 from
# getrandom(2), because a range proof that is not randomised is not a range
# proof. RANDOMIZED keeps the half of the pin that still works -- a value must
# still be printed -- so "the harness never called the module" is still caught.
bulletproofs	ReleaseFast	rangeproof	1	0	3e0167b9b68ce90f	RANDOMIZED
bulletproofs	ReleaseFast	ipa	1	0	dd382e4324993825	2368a5d0e07e2233
paillier	ReleaseFast	crt	1	<=331	8121b95486f955cf	ca16fc01b4ebb44c
paillier	ReleaseFast	noncrt	1	<=141	8121b95486f955cf	ca16fc01b4ebb44c
paillier	ReleaseFast	mul	1	<=12	8121b95486f955cf	b2221403ad3dad41
paillier	ReleaseFast	addm	1	<=10	8121b95486f955cf	526f0de009c0cbfb
# tlock/ibe re-pinned 2026-09-16, after 32e751ad (inversion-free Miller loop):
# tlock fp12pow 8 -> 2, tlock decrypt 11 -> 3, ibe decrypt 14 -> 7. The counts
# FELL because the Miller loop stopped inverting -- 32e751ad dropped
# `two_y.inv()` and `t.x.sub(q.x).inv()` from the per-step line evaluation, so
# those branches went with them. The remainder was read stack by stack before
# this re-pin: tlock fp12pow = tlock.zig:358,361, the module's OWN windowed Gt
# ladder (audit F3), NOT the pairing; tlock decrypt = tlock.zig:406,430,432;
# ibe decrypt = ibe.zig:474,497 over the pairing substrate (pairing.zig:223,
# 270,278,283,432), which makes no constant-time claim of its own.
#
# ⛔ Checked explicitly, because a count that only falls looks identical either
# way: both harnesses still print a real plaintext (`message=5a5a..` for tlock,
# `abab..` for ibe), so this is NOT the "the harness stopped reaching the
# module" failure the seventh column exists to catch. `ibe extract` and `ibe
# fp12pow` were green throughout and are left untouched.
tlock	ReleaseFast	fp12pow	1	2	4ddb26ab436730a6	b904bd817d356db7
tlock	ReleaseFast	decrypt	1	3	4ddb26ab436730a6	3a939f7ff5b39d03
ibe	ReleaseFast	extract	1	3	35d0138e95b4dcf1	686f0732e74c5ef7
ibe	ReleaseFast	decrypt	1	7	99a9b799453f052c	fb52b6c55cc51706
ibe	ReleaseFast	fp12pow	1	2	35d0138e95b4dcf1	d2e187260f9f18a2
frost	ReleaseFast	commit	1	8	cf6d181343529b92	94a2e96655c4ce21
frost	ReleaseFast	sign	1	5	cf6d181343529b92	8484bd50690a78db
musig2	ReleaseFast	sign	1	<=101	a4d62ba84a4d5c2f	2a2f275cd0c5c7c5
# taproot secret 10 -> 9 (re-pin 2026-09-15). The row was already stale at
# 0c0ef527: a --stacks run of that tree reads 9, with the SAME context set as
# today. The source pin never moved, so the drop came from a dependency before
# that commit. The nine are bip340's scalar canonicality/`isZero` checks
# (root.zig:138/168/169, common.zig:75), comb `rejectIdentity` x2
# (group.zig:399), the public key's Y parity (bip340 root.zig:200), and
# canonicality on taproot's own root.zig:274/281.
taproot	ReleaseFast	secret	1	9	9a99856cbd266475	966e0c7b70c18936
adaptor	ReleaseFast	presign	1	<=90	ec23485c29a9e90b	d3d7ee2d921fc316
adaptor	ReleaseFast	adapt	1	2	ec23485c29a9e90b	6e42ff967657d98e
adaptor	ReleaseFast	extract	1	3	ec23485c29a9e90b	76dc1be38fd7746f
spake2plus	ReleaseFast	w0w1	1	4	98d3a68346d3d767	5530136493ed4097
spake2plus	ReleaseFast	computel	1	3	98d3a68346d3d767	37d06a2f53487905
spake2plus	ReleaseFast	proverstart	1	7	98d3a68346d3d767	5b3e4131495365d6
spake2plus	ReleaseFast	verifierstart	1	7	98d3a68346d3d767	ce61814fc8e1b2b7
spake2plus	ReleaseFast	proverfinish	1	12	98d3a68346d3d767	5cdf0fe20429a208
spake2plus	ReleaseFast	verifierfinish	1	10	98d3a68346d3d767	dbdff43379a90cf5
bbs	ReleaseFast	sign	1	<=47	d86719a46eb0093c	2f5b06d6bd92ab96
bbs	ReleaseFast	proofgen	1	<=134	d86719a46eb0093c	01535db4bf6b0ff6
coconut	ReleaseFast	authority_sign	1	<=69	04e312f73ccbddb6	3492f1201b641f09
coconut	ReleaseFast	user_issue	1	3	a6281f1d04b2a243	af24664cb307a403
coconut	ReleaseFast	user_show	1	<=51	04e312f73ccbddb6	c75d6d631d2311f7
hpke	ReleaseFast	x25519_decap	1	1	ceef922c5fee3703	c0cee0a8c6bf72c3
hpke	ReleaseFast	x25519_authdecap	1	2	ceef922c5fee3703	05af25d1d0e3943d
hpke	ReleaseFast	p256_decap	1	2	ceef922c5fee3703	851ee3406999dacc
hpke	ReleaseFast	p256_authdecap	1	4	ceef922c5fee3703	4b0f0c964acb8411
hpke	ReleaseFast	p384_decap	1	2	ceef922c5fee3703	6b70bba9cc229a39
hpke	ReleaseFast	p384_authdecap	1	4	ceef922c5fee3703	2741ac2ff5069767
hpke	ReleaseFast	open	1	1	cd2271d9a14497cf	59883c70dcd08ee6
signal	ReleaseFast	sign	1	1	cce965f3aeb59837	b2a0053066d93a75
signal	ReleaseFast	ratchet	1	2	9e8bc13ebe2fbdb4	92ab00fad0ce5c41
# sphinx construct 10 -> 8 (re-pin 2026-09-15): its two ECDH `k256.mul` calls
# (core.zig:124/:152) read 2 contexts each on the old ladder and 1 each now --
# `rejectIdentity` at group.zig:347 via `mulInner`, exactly k256/mul's 2 -> 1.
# The rest are unchanged: comb `rejectIdentity` x2, std scalar canonicality x2,
# std `cmovznzU64` x2.
sphinx	ReleaseFast	construct	1	8	e6f03464d0ef9fbd	cb364f94f78ea089
sphinx	ReleaseFast	process	1	<=43	e6f03464d0ef9fbd	e7c50d755f64b027
bolt3	ReleaseFast	derive	1	7	d4906365cc4de792	c0fc92c5fbd8fb55
bolt3	ReleaseFast	revocation	1	10	d4906365cc4de792	bc9bf12a55a312c4
bolt3	ReleaseFast	shachain	1	0	d4906365cc4de792	0516122e43713a52
bolt3	ReleaseFast	shachain_index	1	1	d4906365cc4de792	21055013990ce08a
ctap2pin	ReleaseFast	ecdh	1	4	f5de368a2c111ccf	4596df5a3f29ae3b
ctap2pin	ReleaseFast	one	1	7	f5de368a2c111ccf	12de2588e792c80e
ctap2pin	ReleaseFast	two	1	7	f5de368a2c111ccf	4f76909ff534c3cc
ctap2pin	ReleaseFast	token	1	0	f5de368a2c111ccf	076a3a6729a924b0
fss	ReleaseFast	gen	1	0	3a5ad9154e5aa436	b40b02f9cf423f05
# Was `>=1` and a confirmed defect: `dpf.zig:326`'s xorMasked compiled to
# `test $0x1,%r9b` / `je` on the secret control bit at THIS call site while
# staying branch-free at genWithSeeds'. Fixed by laundering the mask through
# an optimization barrier (dpf.zig `blackBox`), so 0 is now the claim and the
# barrier is what holds it -- deleting it puts the branch back.
fss	ReleaseFast	eval	1	0	3a5ad9154e5aa436	da79011b1ab46d96
bfv	ReleaseFast	keygen	1	<=29	b3ff258fd2f8dd50	85d334608120f4fc
bfv	ReleaseFast	encrypt	1	<=17	b3ff258fd2f8dd50	e4cdbb1aa2e9c9dc
bfv	ReleaseFast	decrypt	1	<=6	b3ff258fd2f8dd50	51de1d1011ed3c4c
tfhe	ReleaseFast	keygen	1	0	476357ebc239682e	faee3ac1f09c123f
tfhe	ReleaseFast	encrypt	1	0	476357ebc239682e	aca01c505d98bd94
tfhe	ReleaseFast	decrypt	1	<=10	ff62a60d16b52a66	b6d58329e7c93b2f
tfhe	ReleaseFast	bootstrap	1	<=186	c546ed4f83700e54	4e39213562dbdc80
dkg	ReleaseFast	coeffs	1	<=98	3f546b9ec9ff000d	ace23d0e40b2c8f5
dkg	ReleaseFast	combine	1	0	9d983ff8e2433712	6407896882d3491c
# aescbc — the two padding strippers, the only place this module does its own
# arithmetic over a secret. ⭐ The claim these rows test is NOT "zero": both
# functions END in `if (invalid != 0) return error.InvalidPadding` and return
# `buf.len - pad_len`, and BOTH of those are disclosed to the caller by the
# API's contract (accept/reject, and the plaintext length). The 1 is exactly
# that decision, at `root.zig:170` — measured, not assumed. What the module's
# own words forbid is an EARLY EXIT from the scan or a distinct signal per
# failure reason, and the scan loop contributes 0 contexts, so there is neither.
# ⚠ The same `u1`-flag idiom did NOT survive the compiler in fss
# (`dpf.zig:326`), which is why this is measured rather than reviewed.
aescbc	ReleaseFast	pkcs7	1	1	e007b2ab5512efa3	c4046968725e140d
aescbc	ReleaseFast	xmlenc	1	1	e007b2ab5512efa3	c4046968725e140d
# ── sealedbox: the secret-key text codecs ──────────────────────────────────
# ⭐ These four were 8 / 7 / 43 / 47 until 2026-09-09, all of it std's
# table-driven base64 and hex reached with a SECRET argument: 95 of the 105
# contexts were reported on a LOAD, and the disassembly showed
# `movzbl <table>(%reg)` over a secret byte and over a secret 6-bit group --
# the cache-timing class of T-table AES. The module now carries its own
# table-free codecs for SECRET material only; the public-key codecs still use
# std, deliberately, because their input discloses nothing.
#
# The ENCODERS are pinned at exact 0: they are pure arithmetic, and the only
# indexed load left in the disassembly is `key[i]` over the loop counter.
#
# ⛔ The PARSERS are pinned at 1 and a 0 there would be WRONG. The remaining
# context is `if (invalid != 0)` -- the accept/reject decision the function
# returns to its caller anyway, same reasoning as aescbc above. What the
# constant-time claim forbids is an early exit that reveals WHICH character
# was bad, and the decode loops contribute 0 contexts, so there is none.
#
# ⭐⭐ The out_sha values did not move by a single bit across the fix: the
# branch changed, the result did not.
#
# ⚠ Measured, not reviewed -- and the reason is on this very module. The first
# draft of `ctEq` put the optimisation barrier on the INPUT, which hides the
# value but not the structure; LLVM still turned `ctEq(c,'+') & 62` into a
# `test`/`je` at `root.zig:292`. Only moving the barrier onto the resulting
# MASK removed it. Source review would not have found that.
#
# ⛔⛔ AND THE COUNTS ALONE WOULD NOT HOLD THIS. Verified by deploying the
# defect: with `blackBox` deleted from `ctEq`/`ctGe`, only `b64dec` moves
# (1 -> 5). `hexenc`, `hexdec` and `b64enc` keep their exact counts and are
# caught ONLY by the src_sha column. So on three of these four rows the source
# pin is the whole gate, which is why the guard-stripping fix that gave bolt3
# its pin back matters here too -- a row with NO-OWN-SRC and an unchanged count
# would have let the barrier be deleted in silence.
sealedbox	ReleaseFast	hexenc	1	0	9b2bc6d6cbb1256f	677e9993da327f0d
sealedbox	ReleaseFast	hexdec	1	1	9b2bc6d6cbb1256f	1c8749621d19a09b
sealedbox	ReleaseFast	b64enc	1	0	9b2bc6d6cbb1256f	ec1bc04ea2ea3ff7
sealedbox	ReleaseFast	b64dec	1	1	9b2bc6d6cbb1256f	1c8749621d19a09b
# ── pir: the query index, and the client's check ───────────────────────────
# A1 `pir.md` M2 -- the module's central claim had never been machine-checked,
# and its sibling `fss` exposed a REAL defect the day it entered the gate.
#
# ⛔ Both rows are pinned at 1 and a 0 would be WRONG on both.
#  * `query` 1 = `pir.zig:146` `if (index >= domain_size)`, a contract check on
#    a PUBLIC bound whose outcome the caller already knows (it chose the index).
#  * `reconstruct` 1 = `verify.zig:453` `if (diff != 0) return AnswerRejected`,
#    which SPEC.md calls "the protocol's own public output" -- the abort is
#    visible by definition.
# What the claim forbids is a branch revealing WHICH word mismatched, and the
# fixed-trip loops contribute 0. That is the SPEC sentence, measured.
#
# ⚠ The PRG is pinned to Sha256Prg, NOT the default Aes128Mmo, whose
# `constant_time` is `aes.has_hardware_support`. On an AES-NI host the default
# would measure green for a reason that does not hold on a soft-AES host, and
# SPEC.md says the soft fallback leaks the query index. A row whose colour
# depends on the gate's CPU says nothing. ⛔ So a green `query` row is NOT a
# claim about the default instantiation on a soft-AES target.
pir	ReleaseFast	query	1	1	52e6bf4c59a631f9	0dba0d89ed6fb4ab
pir	ReleaseFast	reconstruct	1	1	d3e3564c69a176d3	b6ecd96f96e9b339
# ── opaque and bip32: first harnesses, 2026-09-16 (A1 opaque M5, bip32 M6) ──
# First pinned 2026-09-15. Each target taints ONE party's secrets, and the
# other party runs untainted first, so nothing public on the wire counts as
# secret. The non-zeros are itemised in each module's SPEC.md § constant time.
# ⚠ opaque's source pin hashes only modules/opaque/src/root.zig, while the
# pattern also matches voprf's and ct25519's root.zig by basename. An edit to
# voprf does NOT turn these rows red by digest; re-run `--check opaque` after
# one.
opaque	ReleaseFast	register	1	4	e450b1638d83989c	843c12b053124a78
opaque	ReleaseFast	login	1	11	e450b1638d83989c	f8be3480eb708638
opaque	ReleaseFast	serverke2	1	2	e450b1638d83989c	4ef279b842b3cb55
# bip32 mnemonic 3 is the M6-CT tree (8c2f1aa4, constant-time `wordIndex`).
# The three are the mnemonic split plus two accept/reject verdicts, not the
# word search.
bip32	ReleaseFast	master	1	1	da144758a15f2f06	7e14466ed98567fa
bip32	ReleaseFast	derive	1	5	da144758a15f2f06	8b410bdbd3955f08
bip32	ReleaseFast	seed	1	0	008c60b09bb381e3	9e74b23db4ad7bdc
bip32	ReleaseFast	mnemonic	1	3	008c60b09bb381e3	d42db8cf9cdc2200
