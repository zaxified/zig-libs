# zig-libs

[![CI](https://github.com/zaxified/zig-libs/actions/workflows/ci.yml/badge.svg)](https://github.com/zaxified/zig-libs/actions/workflows/ci.yml)

A curated collection of **foundational Zig modules** — performance-minded, universal where
possible, each modeled after a proven implementation in another language rather than invented from
scratch.

Not a dumping ground: ship **solid, not many**. Every member is a foundational,
cross-project-reusable capability — a production-grade implementation of a protocol/format/algorithm,
or a fill for a genuine gap in the Zig ecosystem. zig-libs is the canonical home for these; the
authors' other projects depend on it, not the reverse.

**Status:** 211 modules (Zig 0.16, green in Debug + ReleaseFast) · **MIT** (see `LICENSE`;
third-party-derived wire formats & required attributions in `NOTICE`).

## Using a module

- **Local path (dev, no tags/push):** in the consumer's `build.zig.zon`,
  `.zig_libs = .{ .path = "../zig-libs" }`, then in `build.zig`
  `exe.root_module.addImport("http", b.dependency("zig_libs", .{}).module("http"));`
- **Fetch:** `zig fetch --save git+https://github.com/.../zig-libs` then the same `dependency().module(...)`.
  (`zig fetch` can't target a subdirectory — ziglang/zig#23012 — so the whole repo is one package;
  you still import only the module you name. Unused modules aren't compiled.)
- **Pin for reproducible builds:** `zig fetch --save git+https://github.com/.../zig-libs#<tag-or-commit>`
  — pin a release tag or commit, never a branch; `CHANGELOG.md` says what each release changed.

## Build

```
zig build test           # run all module tests
zig build test-<name>    # run one module's tests
zig build check-catalog  # verify build.zig's module_list ↔ modules/ ↔ this README agree
```

## Versioning & stability

One semver for the whole collection — releases are git tags, there are no per-module
versions. Pre-1.0 semantics: a minor bump may break any module's API; a patch bump is
fixes-only. Per-release changes live in `CHANGELOG.md`, grouped by module with breaking
changes flagged. Module maturity is carried by the explicit caveat lines in the catalog
below (and each module's `SPEC.md`), not by stability-tier labels — every module meets the
same bar (tests green in Debug + ReleaseFast, oracle/KAT verification where one exists);
what varies is *scope*, and anything unfinished is stated where it lives. The full
versioning + spin-off policy is `CONVENTIONS.md` §8.

## Layout & conventions

```
build.zig      # single root build — registers every module by name + a test step each
build.zig.zon  # one package manifest for the whole collection
CHANGELOG.md   # per-release changes, grouped by module
CONVENTIONS.md # naming + `meta` tag vocabulary + provenance/SPDX + versioning rules
modules/<name>/src/root.zig  # `// SPDX-License-Identifier: MIT`, `pub const meta`, API, tests
modules/<name>/README.md     # what it is + a Provenance line
```

`CONVENTIONS.md` has the full rules; `modules/_template/` is the starting point for a new module.
Roadmap notes live in the "Roadmap / not yet built" section above and the git history.

## Licensing

zig-libs' own code is MIT throughout. Four modules — `icmp`, `seqmap`, `netaddr`, `dns` — descend
from fping (https://github.com/schweikert/fping), which carries a non-standard Stanford
"BSD-with-advertising" license, not a plain permissive one. That license affirmatively requires
that documentation and advertising materials for redistributions acknowledge the software was
developed by Stanford University. Redistributors of those four modules (or of zig-libs as a whole)
must preserve that fping attribution, reproduced in full in `NOTICE` §1.

## Modules

Every module is imported by its `name` (`@import("http")`); hyphenated names work too
(`@import("security-headers")`). `Deps` are sibling modules; everything else is `std`-only.

### Web / HTTP & API — an internet-facing service, no reverse proxy required

| Module | What it does | Platform | Deps |
|---|---|---|---|
| `http` | HTTP/1.1 client (TLS via `std.crypto.tls`) **and** server, hardened for direct exposure (peer addr, conn caps, size limits, slowloris timeouts, gzip, **conditional requests** ETag/If-\* → 304/412, **request-body parsing** — `Content-Type`, urlencoded, **multipart/form-data** RFC 7578, **Server-Sent Events** encoder + incremental `flush()`, **inbound gzip** request bodies (zip-bomb-capped), **multiple Set-Cookie**, chunked-trailer capture, **`Range` / 206 Partial Content** RFC 7233 — `bytes=` parser + resolve-against-length → `Content-Range` + 206/416 response staging + `multipart/byteranges` body for multi-range requests, **content negotiation** — `Accept` / `Accept-Language` (RFC 4647) / `Accept-Encoding` parsers + `negotiate` server-offers→best-match / 406, RFC 9110 §12.5). Also speaks **HTTP/2** (bidirectional, TLS-deployable) — HPACK (RFC 7541) + framing/flow-control (RFC 9113) + a **DoS-hardened h2c server** + a **multiplexing h2 client** + an **ALPN/bring-your-own-TLS seam**. Not `std.http`. | any | netaddr |
| `router` | REST routing — trie matcher (params/wildcards), middleware chain, groups, 404/405 | any | http |
| `ratelimit` | Token-bucket per-client rate limit → 429 + Retry-After | any | router, http, netaddr |
| `abuseguard` | Per-IP + global connection caps, ban/greylist, strike→ban (accept-time) | posix | http, netaddr, router |
| `throttle` | Global concurrency limit + load-shedding → 503 | posix | router, http |
| `security-headers` | Secure-by-default response headers (HSTS/CSP/nosniff/frame/referrer/COOP/CORP) | any | router, http |
| `cors` | CORS preflight + header injection (secure defaults) | any | router, http |
| `validate` | Request body/query/params validation → aggregated 400 (typed + schema + string `format`: email/uri/uuid/ip/hostname/date-time/…) + **JSON DoS caps** (depth/array/field) | any | router, http, netaddr |
| `metrics` | Prometheus registry (counter/gauge/histogram) + `/metrics` + request middleware + **access-log writer** (combined/JSON) | posix | router, http |
| `health` | Liveness (`/healthz`) + readiness (`/readyz`) probe middleware — 200/503 from registered dependency checks (k8s probe contract) | any | router, http |
| `requestid` | Request/correlation-ID middleware — adopt incoming `X-Request-Id` or generate, echo on the response, expose via `current()` (composes with auth) | any | router, http |
| `tracecontext` | **W3C Trace Context** — `traceparent`/`tracestate` parse + generate + a propagation middleware (child span per hop, `current()`) for distributed tracing | any | router, http |
| `webhooksig` | **HMAC webhook signatures** (GitHub style: `sha256=<hex>`) — `sign`/`verify` (constant-time) + a middleware gating requests by `HMAC-SHA256(secret, body)`, key rotation; Stripe's `t=…,v1=…` scheme is a different construction and is not implemented | any | router, http |
| `idempotency` | **Idempotency-Key** dedup of unsafe retries — a middleware + ramcache-backed `Store` replaying a key's cached response without re-running the handler | any | router, http, ramcache |
| `resilience` | Circuit breaker + retry/backoff + timeout + **bulkhead** (concurrency limiter) for calling upstreams (generic) | posix | — |
| `upstream` | Load-balanced upstream pool + failover — round-robin/weighted/least-conn/EWMA strategies, per-upstream breaker+bulkhead, active+passive health | any | resilience, probe |
| `openapi` | OpenAPI 3.1 spec generated from the route table + `/openapi.json` | any | router, http |
| `aaa-gate` | Bearer + **API-key** auth (constant-time) + audit hook + denied-request throttle | any | router, http |
| `jwt` | JWT/JWS + **OIDC resource-server** validator (RFC 7515/7519/7517/8725) — parse + claims + verify (HS/ES/EdDSA/RSA, alg-confusion-safe) + JWKS-by-`kid` + **OIDC discovery/fetch** (cache + key-rotation) + a **`router` Bearer middleware** (RFC 6750 challenge, scope check, identity on ctx) | any | http, router, p256 |
| `rbac` | authorization decision engine — NIST RBAC (hierarchical + static SoD, cycle-checked) and a depth-bounded ABAC condition-tree evaluator (typed builder, 10 operators) with XACML-style `deny_overrides`/`permit_overrides` combining and structural default-deny | any | — |
| `xml` | namespace-aware, security-hardened XML 1.0 parser → C14N-ready infoset tree (original prefixes/NS-scope/doc-order/comments/byte-spans preserved; DOCTYPE-reject default → XXE + billion-laughs + depth-bomb proof; duplicate-ID guard) — foundation for `xmldsig`/`saml` | any | — |
| `xmldsig` | XML Canonicalization (exclusive/inclusive C14N ±comments, `InclusiveNamespaces` PrefixList) + XML-Signature **verification** (xmldsig-core 1.1) — enveloped-signature transform, RSA/ECDSA-P256-SHA256/384/512, algorithm allow-list (XSLT/XPath rejected), verify-against-configured-key (KeyInfo cert untrusted, exposed for pinning) | any | xml, rsa, p256 |
| `saml` | SAML 2.0 Web Browser SSO **service-provider**: XSW-hardened Response consumption (signed assertion/response verified against a configured IdP key via `xmldsig`, consumed assertion pinned to the signed element by pointer identity), conditions/subject/audience/recipient/InResponseTo validation on a caller-supplied clock, AuthnRequest builder + IdP-metadata parser; **`EncryptedAssertion` decrypted via `xmlenc`** (decrypt-then-verify, XSW held within the decrypted doc) when an SP key is configured | any | xmldsig, xml, xmlenc, rsa, x509 |
| `aescbc` | raw AES-CBC (NIST SP800-38A, over `std.crypto.core.aes`) + PKCS#7 / XML-Enc padding helpers; zero-alloc core, padding-oracle caveat documented (consumers own authenticate-before-unpad) | any | — |
| `aeskw` | RFC 3394 AES Key Wrap (AES-128/256 KEK) — constant-time integrity check + scratch zeroization, byte-exact vs RFC 3394 §4; the canonical impl `jwe`/`dnp3`/`xmlenc` each had mirrored | any | — |
| `xmlenc` | XML-Encryption (xmlenc-core-1) **decryption** — recover SAML `EncryptedAssertion` plaintext: RSA-OAEP (SHA-1/256) / rsa-1_5 (Bleichenbacher-gated) / AES-KW key transport + AES-128/256-GCM/CBC content (CBC core vs NIST SP800-38A, AES-KW vs RFC 3394); decrypt-then-verify, strict algorithm allow-list, CEK zeroized | any | xml, rsa, aescbc, aeskw |
| `jwe` | JSON Web Encryption (RFC 7516/7518) compact serialization — `dir`/RSA-OAEP/RSA-OAEP-256/AxxxKW (RFC 3394)/AxxxGCMKW/PBES2/**ECDH-ES** (+A128KW/A256KW; P-256 + X25519 ephemeral, Concat KDF, RFC 7518 App. C KAT) key management + A128GCM/A256GCM/A128CBC-HS256/A256CBC-HS512 content encryption, RFC-KAT-validated (incl. RFC 7516 A.3 byte-exact both directions); A192* unsupported (no AES-192 in std) | any | rsa, p256, aescbc, aeskw |
| `acme` | Let's Encrypt / ACME v2 (RFC 8555): HTTP-01 issuance + renewal, ES256 JWS, CSR | any | http, router |
| `staticfiles` | path-traversal-safe static file handler over `http` — MIME by extension, ETag / Last-Modified, conditional 304/412, byte-range 206/416; two-layer defense (percent-decode + reject `..`/NUL/backslash → openat-relative single-component `no_follow` resolution, provably contained), symlinks-not-followed + dotfiles-refused by default | any | http |
| `brotli` | pure-Zig Brotli (RFC 7932) — byte-exact decompressor (full: simple+complex Huffman, context modes, distance ring buffer, static dictionary + 121 transforms; validated vs google/brotli vectors) + a minimal valid store-mode encoder; the `Content-Encoding: br` companion to std gzip | any | — |
| `accesslog` | structured HTTP access-log formatter — JSON Lines / logfmt / Apache Combined with rigorous log-injection escaping (untrusted UA/path/referer can't forge a line or inject fields) + an http-request→Entry bridge; caller supplies the timestamp | any | http |
| `websocket` | RFC 6455 WebSocket — opening handshake + frame layer (masking direction enforced, fragmentation, control frames, UTF-8 validation, close codes, per-frame + aggregate size caps), transport-agnostic server + client; permessage-deflate deferred | any | http |
| `sessions` | Server-side web sessions + OWASP-hardened cookies + signed double-submit **CSRF** middleware | any | router, http, cookies, ramcache |
| `llmclient` | Anthropic Messages API client (buffered + streaming SSE) over `http` — no third-party SDK | any | http |

### Networking

| Module | What it does | Platform | Deps |
|---|---|---|---|
| `netaddr` | IP parse/format (RFC 5952) + RFC 6724 source/dest selection + **CIDR/Prefix** ops (contains/overlaps/supernet, range↔prefix summarize) | any | — |
| `dns` | RFC 1035 resolver — A/AAAA/PTR/CNAME/NS/MX/TXT/SOA/SRV/CAA over UDP/TCP + DoH | any | netaddr, http |
| `dnssec` | Resolver-side **DNSSEC validation** (RFC 4033/4034/4035 + NSEC3 RFC 5155; ECDSA RFC 6605, Ed25519 RFC 8080) — DNSKEY/RRSIG/DS/NSEC/NSEC3 RDATA parsing, key-tag + DS digest computation, canonical name/RRset encoding + ordering, per-algorithm signature verify (`std` ECDSA/Ed25519 + the `rsa` module — no crypto primitive reimplemented), NSEC/NSEC3 denial-of-existence | any | dns, rsa |
| `netlink` | rtnetlink **read + write**: dumps (links / addresses / routes / neighbors) **and** `RTM_NEW*`/`RTM_DEL*` writes — address/route add+del, link up/down/MTU/rename/MAC, link create/delete, neighbour add+del; `NLM_F_ACK` with typed errno mapping + extended-ACK reason strings; byte-exact vs iproute2-captured goldens + a netns round-trip | **linux** | — |
| `genetlink` | Generic-netlink (genl) transport: genlmsghdr framing + nlctrl family-id resolution — the shared foundation for genetlink-family clients (ethtool/devlink/nl80211/wireguard) | **linux** | netlink |
| `nl80211` | Wi-Fi control over the nl80211 genetlink family — interface/wiphy enumeration, scan trigger + BSS results, connect/disconnect, station and link statistics, regulatory domain | **linux** | genetlink, netlink |
| `devlink` | Linux devlink over genetlink — device/port enumeration, port split/unsplit, parameter and resource inspection, region snapshots, health reporters and eswitch mode | **linux** | genetlink, netlink |
| `ethtool` | Ethernet device control over the ethtool netlink family — link settings/state, ring/coalesce/pause/channel parameters, feature flags, per-queue and driver statistics, EEPROM/module info | **linux** | genetlink, netlink |
| `wireguard` | Native WireGuard device config over genetlink — get/set device, peers, allowed-ips (retires `wg` shell-outs) | **linux** | netlink, genetlink |
| `tc` | Traffic control over rtnetlink — qdiscs (`netem`, `htb`, `tbf`, `fq_codel`, `mq`, `cake`, `raw`), htb classes, `u32`/`flower` filters and the `gact`/`mirred`/`police`/`skbedit`/`vlan` action families, byte-exact to `iproute2` with read-back verify (retires `tc` shell-outs) | **linux** | netlink |
| `tcplan` | Compile a hierarchical shaping topology (site→AP→subscriber, committed/ceil rates) into a deterministic ordered plan of `tc` ops — `mq` root + per-CPU HTB trees + CAKE leaves + filters, cpumap→MQ→per-CPU aligned; pure (caller executes via `tc`+`netlink`) | linux | tc |
| `nftables` | Typed firewall-ruleset builder → libnftables JSON for `nft -j -f -` (families/chains/rules/sets, match + verdict statements) | any (apply: linux) | netlink |
| `bacnet` | BACnet building automation over **BACnet/IP and BACnet/SC** — BVLL/BVLC framing, NPDU routing header, APDU services (ReadProperty, ReadPropertyMultiple, WriteProperty, WhoIs/IAm, COV subscription), and the ASN.1-style primitive encoding Plus Annex AB Secure Connect: the SC BVLC with VMAC/UUID addressing and header options, a time-injected node and hub connection state machine with heartbeats, backoff and hub failover, over the `websocket` module with TLS as a seam. | any | netaddr, websocket |
| `s7comm` | Siemens S7 communication — ISO-on-TCP (RFC 1006 TPKT + COTP) plus the S7 protocol: connection setup, area read/write (DB/M/I/Q/T/C), PLC info and cyclic services | any | — |
| `enip` | EtherNet/IP + CIP — encapsulation layer (register/unregister session, SendRRData/SendUnitData), CIP messaging (Get/Set Attribute, Multiple Service Packet), connection manager, and a tag/symbolic path client for Logix-class controllers | any | netaddr |
| `iec62351` | IEC 62351 power-systems security — GOOSE/SV authentication (62351-6) over caller-supplied PDU bytes, MMS application authentication (62351-4), and the TLS profile requirements as a checkable policy | any | x509, rsa |
| `iec61850` | IEC 61850 substation automation — MMS (ISO 9506) client over ISO-on-TCP with the ACSI object model, plus GOOSE publish/subscribe encoding and SV sampled values | any | xml |
| `fleetsim` | In-process simulated device fleet — hosts many protocol responders (Modbus, DNP3, IEC 104, S7comm, BACnet, EtherNet/IP, OPC UA) as addressable nodes on one deterministic, time-injected scheduler | any | modbus, dnp3, iec104, s7comm, bacnet, enip, opcua, netsim |
| `smtp` | SMTP client (RFC 5321) — ESMTP EHLO capability negotiation, STARTTLS seam, AUTH PLAIN/LOGIN, pipelining, and MIME message composition (RFC 5322/2045) | any | netaddr |
| `iec104` | IEC 60870-5-104 telecontrol — APCI/APDU framing, I/S/U formats with k/w flow control, ASDU codec for the common type IDs, and a transport-agnostic master (controlling station) | any | — |
| `modbus` | Modbus TCP (MBAP) + RTU (CRC-16) codec, master client **and slave server** — core function codes, diagnostics, exceptions, broadcast/unit-id semantics, transport-agnostic seam | any | — |
| `mqtt` | MQTT 3.1.1 client — all 14 control packets, QoS 0/1/2 state machine, topic-filter wildcards, transport-agnostic seam | any | — |
| `coap` | CoAP (RFC 7252) — a full client/server stack: message codec (header/token/**delta-encoded options**/payload), `options` (registry, CoAP uint, **URI ↔ options** §6), `reliability` (CON **retransmission** §4.2 + message-ID **dedup** §4.5), `client` (URI→request + reply correlation), `server` (dispatch + piggyback/separate responses). Zero-alloc, transport-/clock-agnostic (block-wise + observe are follow-ups) | any | — |
| `snmp` | SNMP v1/v2c — BER/ASN.1 codec + OID + all 8 PDUs + manager client (get/next/bulk/set/walk) + a **trap/notification receiver** (v1 Trap / v2c Trap / Inform → one normalized `TrapEvent` + `Dispatcher` + `ackInform` byte-faithful Response ack) + **SNMPv3** (RFC 3412 message framing + ScopedPDU, plaintext/noAuthNoPriv) + **USM** (RFC 3414) — `UsmSecurityParameters` (de)serializer + **auth**: password→key localization + HMAC-MD5-96 / HMAC-SHA-1-96 sign/verify (constant-time, RFC A.3 KAT-checked; privacy crypto in progress), transport-agnostic seam | any | — |
| `dnp3` | DNP3 (IEEE 1815) base protocol — data-link 0x0564 framing + **CRC-16/DNP**, transport-function segmentation/reassembly, application layer (header/IIN/function codes) + object-header qualifier/range framing + a core object library (binary I/O, CROB, counter, analog I/O, time, class data); master + outstation via the same pure codec. Secure Authentication (g120) is **scaffolded only**, no crypto | any | aeskw |
| `opcua` | OPC-UA (IEC 62541 / OPC 10000) binary **client + server** — the full stack: OPC UA Binary codec + opc.tcp transport framing (caller-supplied stream, never opens a socket of its own), secure channel at `SecurityPolicy#None` or `Basic256Sha256` Sign/SignAndEncrypt (over `rsa`), sessions (Create/Activate/Close), Read/Write/Browse/Call + subscriptions (CreateSubscription / MonitoredItems / Publish); each layer live-validated against open62541 Plus a **server/device side**: a caller-owned address space with the standard ns0 skeleton, a chunk-limit-bounded connection state machine, sessions with anonymous/username identity, and a time-injected subscription/Publish engine (server side is SecurityPolicy#None only); validated live in both directions against open62541. | any | rsa, x509 |
| `whois` | RFC 3912 whois client — query format + referral chasing (IANA→registrar) + field extraction, transport-agnostic seam | any | netaddr |
| `rdap` | RDAP client (RFC 7480–7484) — JSON-over-HTTPS whois successor: query URLs, typed response model, IANA bootstrap, fetch seam | any | http, netaddr |
| `icmp` | ICMP echo (ping) engine — v4/v6 codec, batched socket, pacing | **linux** | seqmap, netaddr |
| `traceroute` | ICMP-echo path discovery — TTL-stepped probes, per-hop address + RTT stats, load-balanced-path aware | **linux** | icmp, netaddr, latency-stats |
| `probe` | TCP-connect reachability prober — up/refused/timeout + RTT, fan-out with bounded concurrency, latency aggregation | any | netaddr, latency-stats |
| `l2disco` | Layer-2 / neighbor discovery codec — LLDP (802.1AB) + CDP + ARP (RFC 826) + DHCP options (RFC 2131/2132) + MAC helper | any | netaddr |
| `isis` | IS-IS (ISO/IEC 10589) PDU codec — common header + TLV framework + IIH/LSP PDUs + SPB (802.1aq) TLVs + raw-TLV escape hatch; pure bounds-checked encode/decode of untrusted link bytes, the wire foundation for an SPB control plane | any | — |
| `isis-adj` | IS-IS point-to-point adjacency state machine (ISO 10589 §8.2 + RFC 5303 three-way handshake) — a pure time-injected FSM driving one P2P neighbour Down→Init→Up from received IIH PDUs (`isis` codec) + hold-timer expiry; no threads/timers/sockets | any | isis |
| `isis-dis` | IS-IS LAN Designated-IS election (ISO 10589 §8.4.5) — from the LAN routers' priority + SNPA elect the DIS (highest priority, SNPA tie-break, preemptive), report whether this system is DIS + derive the pseudonode LSP-ID; pure time-injected, adds LAN to the P2P isis stack | any | isis |
| `isis-lsdb` | IS-IS link-state database — store LSPs by LSP-ID, ISO 10589 §7.3 newer-LSP comparison (seq → zero-lifetime → checksum), time-injected aging + MaxAge purge, per-interface SRM/SSN flag sets for flooding; pure (caller drives I/O), on the `isis` codec | any | isis |
| `isis-flood` | IS-IS flooding transmit scheduler — drain `isis-lsdb` per-interface SRM/SSN flags into the ordered PDUs to send, pace LSP (re)transmission + emit periodic CSNPs; pure time-injected (caller supplies now + Up-adjacency ifaces, does the sends) | any | isis, isis-lsdb |
| `isis-spf` | Compute the IS-IS shortest-path forwarding table from an `isis-lsdb` — IS-reachability TLVs → topology graph (two-way-checked edges) → `spf-ect` Dijkstra + ECT tie-break from the local system → route table (dest system-id → next-hop + metric); pure, deterministic | any | isis, isis-lsdb, spf-ect |
| `isis-sim` | Headless multi-node IS-IS/SPB fabric convergence simulator — a `netsim` Protocol running the `isis-lsdb`/`isis-flood`/`isis-spf` stack on every node, asserting the fabric converges (LSDBs synchronise) and reconverges after an injected link failure; the end-to-end proof the P2P isis stack works together | any | netsim, isis, isis-lsdb, isis-flood, isis-spf |
| `seqmap` | Fixed 65 536-slot 16-bit request/reply correlation map, O(1) | any | — |
| `latency-stats` | Online RTT stats — min/max/mean/stddev + RFC 3550 jitter + loss % (O(1)/sample, no alloc) + an **HdrHistogram** for bounded-error percentiles (p50–p99.9) | any | — |
| `pping` | Passive RTT estimation from TCP TSval/TSecr echo matching (RFC 7323 / Pollere pping) — first-echo match-and-consume, insert-if-first, aging-before-capacity eviction over a bounded per-direction table; no double-counting of duplicate/delayed ACKs | any | — |
| `procnet` | Linux `/proc`+`/sys` parsers — ARP/routes/TCP+UDP sockets/conntrack/process stats/device health, typed | **linux** | netaddr |
| `conntrack` | Linux **ctnetlink** (`NETLINK_NETFILTER` / `NFNL_SUBSYS_CTNETLINK`) client — typed conntrack flow dump/get/delete plus event subscription, over `netlink`'s write engine | **linux** | netlink, netaddr |
| `rawsock` | Linux **AF_PACKET** raw-frame capture + inject — BPF filter, promiscuous mode, typed frame decode | **linux** | netaddr |
| `stun` | STUN client (RFC 8489) — NAT reflexive-address discovery: XOR-MAPPED-ADDRESS + MESSAGE-INTEGRITY + FINGERPRINT | any | netaddr |
| `sntp` | SNTP client (RFC 4330) — NTP packet codec + UDP query, clock offset / round-trip delay | any | — |
| `syslog` | RFC 5424 syslog formatter + emitter, RFC 3164 legacy encoder, RFC 6587 TCP octet framing | any | — |
| `ssh` | SSH-2.0 (RFC 4253) **client + server** transport — version exchange, KEXINIT, curve25519-sha256 (RFC 8731) + diffie-hellman-group + ML-KEM-768 hybrid KEX, Binary Packet Protocol, aes-gcm/chacha20-poly1305; live-validated against OpenSSH. **userauth (RFC 4252) publickey/password + channels (RFC 4254) session/exec/subsystem** — one-shot `exec()` and a streaming `Session` seam | any | rsa |
| `netconf` | NETCONF client (RFC 6241) over SSH — RFC 6242 end-of-message + chunked framing, hello/capability exchange, get/get-config/edit-config/commit RPCs with typed replies | any | ssh, xml |
| `ebpf` | eBPF program generation over `std.os.linux.bpf` — verifier-passing bytecode builders (kprobe counter / XDP bounds-checked filter / ring-buffer emitter, clang-cross-checked goldens); syscall attach + ringbuf consumer scaffolded. Real-kernel verifier acceptance unverified in unprivileged CI | **linux** | netlink |
| `xdp-classifier` | XDP packet classifier for a LibreQoS-style edge shaper — IPv4 subscriber-prefix → traffic-class handle: verifier-safe Ethernet+IPv4 parse, `BPF_MAP_TYPE_LPM_TRIE` longest-prefix-match lookup, per-CPU scratch-map class handoff, plus `BPF_MAP_TYPE_CPUMAP` steering (redirect a matched flow to a chosen CPU via `bpf_redirect_map`). Mechanical composition of `ebpf`'s already-proven bounds-check/map-lookup patterns — no Fable core | **linux** | ebpf |
| `netsim` | Deterministic seeded discrete-event network simulator (VOPR-style: nodes/links/latency/loss/partition/clock-skew, failure-schedule fuzzer, byte-exact replay, ddmin counterexample minimizer) — the model-checking harness for fabric algorithms + fleet-sim | any | — |
| `spf-ect` | Deterministic symmetric shortest-path (Dijkstra) with a reversal-invariant ECT tie-break (`path(A→B) == reverse(path(B→A))`, RFC 6329 idea generalized) + maximally-disjoint second tree (PRP mode). Pure graph algorithm | any | — |
| `loopfree-reconv` | Loop-free reconvergence transitions — two-class ordered-FIB schedule (provably no transient forwarding loop, TTL backstop), netsim-verified across fuzzed reconvergence schedules incl. overlapping transitions | any | netsim, spf-ect |
| `df-elect` | Partition-correct Designated-Forwarder election (static link-state total order, forced by a duplicate-freedom argument — RFC 7432 §8.5 analog) + split-horizon; bounded-badness model-checked in netsim under partition/heal fuzzing | any | netsim |
| `raft` | **Raft consensus** (Ongaro & Ousterhout) — leader election + log replication, model-checked in netsim against all five formal safety properties (Election Safety / Leader Append-Only / Log Matching / Leader Completeness / State Machine Safety) under fuzzed crash/partition/reorder/clock-skew schedules. Wire codecs + log container + timers + protocol plumbing + the five checkers (with synthetic teeth) + a deliberately-broken positive control, plus the irreducible safety core (up-to-date restriction, RequestVote grant, AppendEntries conflict-only truncation, Figure-8 commit rule) — implemented and unit-tested, with the real `RaftServer` holding all five invariants live across the fuzzed seed sweep. Membership changes (§6) design-only (`jointMajority` implemented, not yet wired) | any | netsim |
| `liveness-hyst` | BFD-like link-liveness estimator with EWMA hysteresis — echo-probe timing + jitter/loss stats, Babel-style metric smoothing as an input filter: fast detection without flap-driven oscillation | any | netsim, latency-stats |
| `loopix` | **Loopix mixnet** (Piotrowska et al., USENIX Security 2017 — Nym's design) — Poisson mix + cover traffic over Sphinx, model-checked in netsim against a global-passive-adversary anonymity invariant. The whole anonymity harness is real: the stratified topology + route selection (incl. a real `sphinx` onion carrying per-hop `(next_hop, delay)`, peeled end-to-end), the fixed-size header codec, the **GPA anonymity measurement** (Serjantov–Danezis effective set `2^H` + linking probability under the mix's own delay law), and a deliberately-broken **FIFO positive control** the harness flags hard (every real target pinned, effective set 1). The irreducible **Poisson mixing core** (`sampleExpDelay` / `scheduleRelease` / `nextCover`) is implemented — memoryless geometric holds (constant per-tick release hazard `1/mean`) — and, measured over a clean global-passive-adversary run + steady-state window (`adversary.measure` unchanged), satisfies a retuned `AnonymityBound` with margin (worst-case `min_eff_set 2.80`) while the FIFO **and** no-cover controls fail it under identical measurement (`1.00`/`1.06`) | any | netsim, sphinx |
| `workerpool` | in-process fixed-width worker pool over `lockfree.MpmcQueue` — type-erased closure jobs, Io-futex idle wakeup (no busy-spin, no lost-wakeup), graceful drain / abrupt shutdown; substrate for the write-behind data layer | any | lockfree |
| `shardstore` | key-sharding router over N independent `kvtree` stores — multi-core write parallelism (per-shard single-writer, cross-shard parallel; caller partitions same-shard writes) | any | kvtree |
| `writebehind` | crash-safe write-behind cache coordinator — fast in-memory acks, async flush to a durable `Sink` (vtable; `kvtree`/in-memory reference sinks) via `workerpool`, `jobqueue` WAL written before ack so an acknowledged write survives a crash (`recover()` replays); per-key LWW, single-flight-per-key | any | ramcache, workerpool, jobqueue, kvtree |
| `readthrough` | Backend-agnostic read-through cache coordinator — serve-from-cache or single-flight-coalesce a miss into one backend fetch, TTL + explicit invalidation + negative caching; read-path counterpart to `writebehind` | any | ramcache |
| `pagecache` | bounded write-through page cache between `kvtree`'s pager and its `Storage` — hot-cold tiering (W-TinyLFU via ramcache) with an RSS budget; transparent (a `kvtree.Db` over it behaves identically to one over the raw store) | any | kvtree, ramcache |
| `lockfree` | **Lock-free concurrency primitives** for shared-memory worker pools (Michael & Scott MPMC queue, PODC 1996 + Fraser/crossbeam epoch reclamation) — the workspace's first lock-free structure; immediate consumer = the in-process worker pool. The mechanical layer (typed atomic helpers + backoff, a poisoning node pool whose canary catches use-after-free, the EBR domain/participant storage + registration, the queue node/init/deinit) and the entire real-threads concurrent-stress harness (N-producer/M-consumer multiset invariant — no lost/duplicated/corrupted — with a correct spinlock oracle, a deliberately-racy `BrokenRing` positive control, and deterministic checker + canary teeth) are green in Debug + ReleaseFast. The concurrency-correctness **core is implemented** — EBR `enterCritical`/`exitCritical`/`retire`/`tryAdvance` (the safe-reclaim predicate) + the queue `enqueue`/`dequeue` CAS loops — under a **`seq_cst` discipline** (Zig 0.16 removed `@fence`, so every reclamation-relevant atomic is `.seq_cst`, giving one total order that forbids the store-buffering UAF); the grace-period safety theorem is documented at `ebr.Domain.tryAdvance`. Sanitizers unusable here (no pure-Zig ASan flag; TSan links libc + misses a blatant race) → the poison/canary pool is the in-tree UAF detector, verified to catch a grace-period sabotage 6/6 | any | — |
| `ethfrag` | Hardened inner-frame fragmentation/reassembly codec — RFC 5722 whole-datagram overlap rejection, bounded per-datagram + concurrent-datagram memory, caller-clocked timeout, fuzz-tested never-panic | any | — |
| `l2encap` | Tenant-tagged (24-bit I-SID) L2-over-tunnel encapsulation for a multi-tenant L2VPN fabric — lean versioned header (I-SID + TTL + BUM/split-horizon bits) wrapping a customer Ethernet frame for encrypted-backbone transport; bounds-checked untrusted decode, composes with `ethfrag` | any | — |
| `l2forward` | E-LAN edge forwarding table — per-I-SID customer-MAC learning (MAC → remote PE) with time-injected aging + the BUM ingress-replication set with split-horizon; forward decision (known-unicast PE vs replication set) for a multi-tenant L2VPN fabric; pairs with `l2encap` | any | — |
| `pbb` | IEEE 802.1ah Provider Backbone Bridge (MAC-in-MAC) codec — wrap a customer Ethernet frame in a backbone header (B-DA/B-SA/optional B-Tag) + I-TAG (24-bit I-SID) and decode back; the real-Ethernet SPB (802.1aq) encap, distinct from `l2encap`'s lean over-WG variant; bounds-checked untrusted decode | any | — |
| `bumtree` | SPB per-source loop-free BUM distribution tree + RPF check over `spf-ect` — for an I-SID member set + a source, the per-node replication next-hops (pruned source SPT) + the single RPF ingress; the control-plane loop-free BUM tree `l2encap`/`l2forward` defer to | any | spf-ect |
| `spbfib` | SPB (802.1aq) forwarding addressing — unicast B-MAC FIB from an `isis-spf` route table re-keyed by backbone MAC + SPBM group multicast-DA construction (SPSourceID + I-SID); one congruent ECT path per dest, no per-flow ECMP; the unicast counterpart to `bumtree` | any | isis-spf |

### Data & storage

| Module | What it does | Platform | Deps |
|---|---|---|---|
| `kv` | Crash-consistent embedded KV store (Bitcask-style log + **randomized seeded VOPR**: model-checked crash recovery across fuzzed fault schedules) | any | — |
| `kvtree` | Ordered **transactional** KV store — copy-on-write B-tree (LMDB/BoltDB lineage): MVCC snapshot isolation, multi-key ACID txns, ordered range scans, VOPR-checked crash-safety. Crash-atomicity core (COW commit / crash-recovery meta-selection / reader-safe reclaim) implemented; property + crash-sweep tests run for real | any | kv |
| `ramcache` | Bounded in-memory cache — **W-TinyLFU** admission/eviction (window+SLRU+CMS sketch) + TTL + generation invalidation | any | — |
| `decimal` | Exact i128 fixed-point decimal (money math), float-free — with IEEE/GDA rounding modes, rescale + rounded division | any | — |
| `jobqueue` | Durable background-job queue over `kv` — lease/retry/DLQ, per-partition FIFO under priority, scheduled visibility | posix | kv |
| `blobstore` | Content-addressed blob store (git-object/restic style) + name-addressed + small named-record layers, crash-safe | posix | hashdigest |
| `filestore` | DB-less durable keyed document store — one atomically-written file per record + a typed-JSON convenience layer | posix | — |
| `dataset` | Canonical in-memory columnar-typed table — the normalization seam between data sources and consumers | any | — |
| `tabular` | Dataset algebra (pandas/dplyr-style verbs) over `dataset` — aggregate/pivot/resample/rolling/join, fx-aware | any | dataset |
| `jsonshape` | JSON → `dataset` reshaping — dot-path descent + typed column projection (jq-style minimal subset) | any | dataset |
| `finstats` | Portfolio/financial statistics over `dataset` — XIRR/TWR/risk/beta/Monte-Carlo/correlation matrix | any | dataset |
| `trie` | Prefix index for instant autocomplete over a large static string set | any | — |
| `fuzzysearch` | Bounded-edit-distance typo-tolerant lookup over a static string set — DoS-bounded, the typo-tolerant sibling of `trie` | any | trie |
| `geoindex` | Static spatial index for bbox + nearest-neighbour queries over a large fixed geo-point set — DoS-bounded, frozen zero-copy | any | — |

### Crypto

| Module | What it does | Platform | Deps |
|---|---|---|---|
| `hashdigest` | Streaming digests — one-shot / incremental / file (EOF-read, size-0 `/proc` safe); SHA-256 convenience + a multi-algorithm layer (SHA-2/SHA-3/BLAKE2b/BLAKE3) | any | — |
| `ripemd160` | RIPEMD-160 (ISO/IEC 10118-3) — std-crypto-style `init`/`update`/`final` streaming hash (little-endian length padding, unlike SHA-2's BE) + `hash160` (`RIPEMD160(SHA256(x))`, the Bitcoin pubkey-hash primitive) | any | — |
| `bech32` | **Bitcoin address encodings** — bech32 (BIP173) + bech32m (BIP350) generic codec (BCH checksum, HRP expansion, charset), segwit address encode/decode (`encodeSegwit`/`decodeSegwit`: witver 0-16, program 2-40 bytes, v0 must be 20/32 bytes, v0=bech32/v1+=bech32m variant enforced), and base58/base58check (Bitcoin Core's bignum algorithm) + P2PKH/P2WPKH convenience over `ripemd160.hash160`. Byte-exact against the official BIP173 + BIP350 valid/invalid test-vector appendices (mixed case, bad checksum, wrong variant, bad padding, bad program length all rejected with a specific typed error) plus a known mainnet P2PKH base58check vector; no allocation, fail-closed on untrusted address strings | any | ripemd160 |
| `sealedbox` | NaCl `crypto_box_seal` — anonymous-sender X25519 public-key encryption (thin over `std.crypto`) + base64/hex key serialization | any | — |
| `minisign` | **minisign file format** (jedisct1/minisign) — a compact GPG alternative for signing files/releases: Ed25519 sign/verify over the `untrusted comment:`/base64 text framing, both legacy (`"Ed"`, raw file) and modern prehashed (`"ED"`, BLAKE2b-512 digest) signature algorithms, the authenticated trusted-comment global signature, and scrypt-encrypted secret keys (ops/mem-limit → `(N,r,p)` via `std.crypto.pwhash.scrypt.Params.fromLimits`, matching libsodium's `pickparams` identically). **Byte-exact KAT-validated against real output from the `minisign` 0.12 reference binary** — parse+verify AND re-sign-reproduces-the-same-bytes, both signature algorithms, both an unencrypted and a real password-encrypted secret key — not just self round-trips | any | — |
| `rsa` | Pure-Zig RSA (PKCS#1 v2.2, RFC 8017) over `std.crypto.ff` — public + CRT-private keys, PKCS1-v1_5 **and PSS** sign/verify, **OAEP** + PKCS1-v1_5 encrypt/decrypt, **keygen**, DER/PEM + **OpenSSH** (bcrypt-pbkdf) key parsing, X.509 self-signed certs; OpenSSL-KAT-validated | any | montint |
| `x509` | X.509 **certificate-chain / path validation** (RFC 5280 §6) — the multi-certificate trust verifier `std.crypto.Certificate` lacks: chain building against a trust store, the extension parsing std omits (basicConstraints / keyUsage / extKeyUsage / SKI-AKI), validity + signature + name checks along the whole path. Consumers: mutual-TLS client-certificate auth, OPC-UA trust lists | any | rsa |
| `paillier` | **Paillier additively-homomorphic PKE** (P. Paillier, EUROCRYPT 1999) over `std.crypto.ff` — 2048-bit keygen, encrypt/decrypt via the `g = 1+n` binomial form, and the homomorphic ops `E(m₁)·E(m₂)=E(m₁+m₂)` / `addPlaintext` / `E(m)ᵏ=E(k·m)`; the L-function decrypt rejects non-unit ciphertexts fail-closed, const-time secret paths (decrypt `c^λ`, keygen). Value-exact cross-checked against `phe` (python-paillier) + exhaustive round-trip / homomorphic-property tests. The foundational sub-primitive for GG20/CMP threshold-ECDSA (Phase 2 — ZK/range proofs + MtA out of scope here) | any | montint |
| `threshold_ecdsa` | **GG20 threshold ECDSA over secp256k1 — Phase 2a: trusted-dealer keygen** (`splitSecretKey` / `groupPublicKey` / `derivePublicKeyShare`) — Shamir + Feldman VSS over the secp256k1 scalar field, per-party Paillier keypairs (via `paillier`), and the ring-Pedersen auxiliary-parameter structs the GG20 ZK proofs consume; any `t`-of-`n` shares reconstruct the key and the group public key `X = x·G` (Feldman-consistent). **Phase 2b** adds the ring-Pedersen `generateAuxParams` (safe-prime `N_tilde`, `h1`, `h2=h1^λ`) and the semi-honest **MtA** (multiplicative-to-additive share conversion over `paillier`: `α+β ≡ a·b mod q`). **Phase 2c** adds the GG18 Appendix-A MtA zero-knowledge range proofs — Alice's range proof (Πᵢ), Bob's MtA proof (Π^MtA), and the MtAwc "with check" variant — over the ring-Pedersen commitments with a SHA-256 Fiat-Shamir transcript, plus the fail-closed `mtaAliceFinalizeChecked` path, verified against the GG18 paper with security-critical reject tests (out-of-range `a`/`b` at the exact q³/q⁷ bounds, tampered ciphertexts, cross-protocol reuse). **Phase 2d** completes the arc with the GG20 online signing protocol (per-pair MtA/MtAwc for the `k·γ` and `k·x` additive shares → `δ` → `R` → `s`), producing a **standard secp256k1 ECDSA signature that `std.crypto.sign.ecdsa` verifies under the group key** (the decisive end-to-end test); identifiable-abort is fail-closed "abort-only v1" (culprit-naming deferred). The ring-Pedersen **aux-param proofs-of-correct-generation** (`aux_proofs.zig` — CGGMP21 Fig.16/17: Πmod Paillier-Blum-modulus + Πprm ring-Pedersen-parameter ZK proofs) **close audit F1**: a generator proves its `(Ñ, h1, h2)` tuple well-formed (`proveWellFormed`) and a validator rejects crafted params — a 3-prime `Ñ` or an `h2 ∉ ⟨h1⟩` pair — that slip past `validate`'s necessary-but-insufficient Jacobi floor (`verifyWellFormed`), soundness carried by the per-round `z^N`/`x^4` (Πmod) and `s^z = A·t^e` (Πprm) checks. NB: the GG18/GG20 ZK proofs' soundness against an adaptive malicious prover is not fully provable by self-tests — SPEC documents the residual review debt; external audit is warranted before production MPC-custody use | any | paillier, montint |
| `dkg` | **Secure Distributed Key Generation (GJKR) for `threshold_ecdsa`** over secp256k1 — removes the trusted-dealer assumption: `n` parties jointly generate an ECDSA secret sharing with no dealer, closing `threshold_ecdsa`'s standing audit debt. The bias-resistant Gennaro-Jarecki-Krawczyk-Rabin construction (J. Cryptology 2007): Round 1 Pedersen-VSS deal (`C_ik = g^{a_ik} h^{b_ik}` broadcast + `s_ij`/`s'_ij` shares), Round 2 complaint → **QUAL** from complaints alone, then — *only after QUAL is fixed* — Feldman-commitment reveal and public-key extraction `Q = Σ_{i∈QUAL} g^{a_i0}` (committing before revealing is what denies a rushing adversary the ability to bias `Q`). Output (`DkgShareOutput`) feeds the REAL `threshold_ecdsa.signWithShares` via `assembleKeyShares`, so the **end-to-end anchor** is: DKG → threshold sign → `std.crypto.sign.ecdsa` verify under `Q` (a valid std-verifiable signature proves the dealer-free key is correct + usable, no external KAT needed). **Status — complete**: the five irreducible protocol-soundness functions (share-vs-Pedersen/Feldman verification, complaint/QUAL/disqualification, bias-prevented extraction, share combination) are implemented and `gate.fable_core_implemented` is `true`, so the end-to-end anchor and the adversarial round-sim run for real; everything else (commitment helpers, wire codecs, the synchronous adversarial round driver, invariant checkers, `BrokenDkg` positive control, anchor wiring) is real too. The positive control still keeps the harness honest (accepts a Byzantine bad share → `reconstructsToQ` catches the unusable key). Phase 1 = secret-key DKG; identifiable-abort / proactive refresh / aux-param DKG are later increments | any | threshold_ecdsa, paillier |
| `vdf` | **Wesolowski Verifiable Delay Function** over an RSA hidden-order group `Z_N*` (Wesolowski, IACR ePrint 2018/623) — `eval(N,x,T) = x^(2^T) mod N` as `T` **sequential** modular squarings (the unshortcuttable delay, over the RSA-2048 Factoring Challenge modulus as the zero-trusted-setup reference group), a Fiat-Shamir `hashToPrime(N,x,y,T)` challenge prime (SHAKE256 candidate + deterministically-seeded Miller-Rabin so prover/verifier agree on `l` cross-machine), and the two Fable cores: **`prove`** computes `π = x^⌊2^T/l⌋ mod N` via a **streaming long-division quotient** (one dividend bit per squaring, no big-int, ≤2× `eval`'s work) and **`verify`** accepts iff `π^l·x^r == y (mod N)` with `r = 2^T mod l` — the Fiat-Shamir binding **includes `y`** (the soundness crux: binding only `(N,x,T)` would let a prover pick `l` in advance and forge). Untrusted `x`/`y`/`π` validated fail-closed (`0`/`N`/`≥N` rejected, never UB). **Trusted-setup caveat documented**: whoever knows `N`'s factorization shortcuts the delay via `2^T mod λ(N)` — hence the challenge modulus; a caller-supplied `N` requires trust, class-group is the no-trusted-setup follow-up. No public byte-exact RSA-Wesolowski vectors exist (all refs are class-group) — verified via property + soundness KATs, a naive-vs-streaming big-int cross-check across the `q=0`/`q=1` boundaries, and Python-oracle-verified `eval`/`hashToPrime` | any | montint |
| `montint` | **Constant-time Montgomery modular arithmetic over arbitrary ODD moduli** (prime or composite: RSA/Paillier `N`, VDF group order, pairing-field prime) — the performance-competitive native-Zig alternative to `std.crypto.ff`, which reserves a carry bit per limb (63-bit redundant limbs) + 4-way half-limb `mulWide`, forfeiting the host add-with-carry/widening-multiply and costing 8–29× OpenSSL across this repo's `rsa`/`paillier`/`vdf`. `Modint(max_bits)` uses **full radix-2^64 limbs**: `toMontgomery`/`fromMontgomery`, modular `add`/`sub`/`mul`, and a constant-time 5-bit-window `powMont`. The portable **CIOS** Montgomery multiply (Koç et al.) is the correctness ORACLE — byte-exact vs an independent CPython-bignum reference at 256/512/2048/4096-bit — and the fallback on every non-amd64 target. On x86-64+ADX+BMI2 the irreducible core is the hand-written **`MULX/ADCX/ADOX` dual-carry-chain CIOS** (the OpenSSL `x86_64-mont5` technique: `MULX` widening products touch no flag, `ADCX`/`ADOX` run two independent carry chains through CF/OF, `dec`-driven loop control preserves CF, OF folded before each `dec`), verified against the portable oracle by a live differential (5000 random 2048-bit cases + an `n`-sweep over `{1,2,3,4,5,8,16,17,32,33,64}` limbs incl. leading-zero-limb moduli + squaring aliasing, all bit-exact) plus fault-injection positive controls. CT-checked in the ReleaseFast disassembly: every branch is on the PUBLIC limb count, the final subtract is a `setb`+mask select (no secret `Jcc`). **Small-L dispatch** (`asm_min_limbs=32`): the asm is ~2.4× slower than the comptime-unrolled portable path at 256-bit and only wins from ~2048-bit up, so moduli `<2048-bit` — including the `bn254`/`bls12_381` pairing fields (L=4/6) — stay portable; asm serves the large RSA/Paillier/VDF moduli. Shipped path this host: 2048-bit modmul **1.32×** / modexp **1.79×** OpenSSL full-width (beats OpenSSL at 256-bit), vs the 8–29× the `ff`-backed modules pay | x86-64 asm + portable fallback | — |
| `chachapoly` | **SIMD-accelerated ChaCha20-Poly1305 AEAD (RFC 8439) — a throughput-specialized duplicate of `std.crypto.aead.chacha_poly`.** `std`'s ChaCha vectorises *within* one block (diagonal `@shuffle`) but gates its AVX2/AVX-512 lanes behind `builtin.cpu.has(.x86, .avx2)`, so at the default `baseline` x86-64 target it collapses to a single 128-bit lane (degree 1) and runs ~3.9× slower than OpenSSL AVX2. This module uses the **block-parallel (transpose) layout**: each of the 16 ChaCha state words is held in a `@Vector(8, u32)` spanning 8 consecutive counter blocks, so a quarter-round is 8-way data-parallel with no in-block shuffles (`@Vector(8,u32)` → 256-bit AVX2 where enabled, paired SSE2 on baseline); the `<8`-block tail runs a scalar `N=1` pass over the same engine. **Poly1305 is scalar** — it reuses `std.crypto.onetimeauth.Poly1305` verbatim (the ChaCha keystream dominates AEAD cost, so the scalar MAC already banks most of the win; a SIMD Poly1305 is a backlog item). Byte-exact anchors: the RFC 8439 §2.3.2 block / §2.4.2 encrypt / §2.5.2 Poly1305 / §2.8.2 AEAD KATs, **and** a differential against `std.crypto.aead.chacha_poly.ChaCha20Poly1305` (the oracle) over every block-boundary edge length {0,1,63,64,65,255,256,257,511,512,513,…} — ciphertext + tag byte-identical, cross-decrypt both ways, tamper rejected — in Debug **and** ReleaseFast. Constant-time (add/xor/rotate/mul only, no secret index or branch; tag compare via `timing_safe.eql`). API mirrors std so `signal`/`noise`/`wireguard`/`hpke`/`quic-crypto`/`bolt8` can swap it in. This host (AVX2, 8 KiB): keystream **2374 MB/s** vs std 1486 (**1.6×**, above OpenSSL's 1852) · full AEAD **662 MB/s** vs std 507 (**1.3×**, capped by the scalar MAC) | any (SIMD via `@Vector`) | — |
| `aeadframe` | Per-key AEAD record layer — seal/open with a monotonic counter nonce (never reused), epoch rekey, sliding-window anti-replay + AAD binding; generic over the AEAD (ChaCha20-Poly1305 via `chachapoly`, AES-256-GCM via std). S1b per-tenant L2VPN confidentiality (one channel per I-SID) | any | chachapoly |
| `tenantkex` | Per-tenant key exchange — a Noise_IK handshake (via `noise`) between two provider edges with the I-SID bound into the prologue, deriving the two directional channel keys that feed `aeadframe`; pure driver, caller carries the messages over the WG backbone | any | noise |
| `k256` | **asm-accelerated secp256k1 — MULX/ADX Solinas field + GLV verify; bit-exact vs `std.crypto.ecc.Secp256k1` + BIP340; ~2–3.5× libsecp256k1 verify, sign pending comb table.** std's secp256k1 is portable pure-Zig (fiat Montgomery field, no asm, no GLV), ~9–14× slower than libsecp256k1, and the repo's 8 Bitcoin/Lightning modules all ride it; k256 is our own speed-specialized secp256k1 keeping the zero-C/no-libc invariant. The base field `Fe` over `p = 2^256 − 2^32 − 977` uses the curve-specific **Solinas reduction** (fold `2^256 ≡ 2^32+977`) on four full 2^64 limbs — a portable wide-integer version is the correctness ORACLE (byte-exact vs `std.crypto.ecc.Secp256k1.Fe`), and on x86-64+ADX+BMI2 the irreducible core is a hand-written **`MULX/ADCX/ADOX` dual-carry-chain** 4×4→8-limb multiply + dedicated square with a branch-free limb-level fold (`<2^290→<2^257→<2^256+c→<2^256`) and a masked `sbb`+XOR-blend final reduce. The group is homogeneous-projective Renes–Costello–Batina complete formulas (same as std) over `Fe`, byte-exact vs std at the point level; the second core is the **GLV endomorphism** variable-base + double-base public multiply (`k=k1+k2·λ`, `φ(x,y)=(β·x,y)`, interleaved width-5 wNAF — ~half the doublings) that the verifier's `s·G−e·P` rides. Anchors: field/group/scalar differentials vs std on thousands of random + adversarial-edge inputs (0/1/`c`/`p−1`/`p−c`/single-bit/max-limb), the live core-vs-portable gated differential (asm==portable, GLV==CT double-and-add), the 19 official **BIP340** vectors (8 sign byte-exact, all 19 verify TRUE/FALSE), ECDSA accept-std-signed/reject-tampered, and a deliberately-broken Solinas-constant positive control that stays RED-catching. Fault-injected (wrong fold constant, dropped MULX carry, broken square doubling, flipped wNAF sign) — each goes RED on the differential + BIP340. Field mul/sq run on SECRET signing scalars: CT-checked in the ReleaseFast disassembly (zero secret-dependent Jcc/cmov/setcc, fixed-offset loads only, masked final reduce); GLV is vartime/public-only per contract. This host: field mul ~2.9× / verify ~2.5× libsecp (~6.5× std). **Sign is still ~10× libsecp** (two naive CT base-point ladders) — the fixed-base comb/precomputed-`G` table is the sign fix and a scheduled follow-up | amd64 asm + portable fallback | — |
| `p256` | **asm-accelerated NIST P-256 — MULX/ADX Solinas field + comb-CT sign + wNAF vartime verify; bit-exact vs `std.crypto.ecc.P256` + RFC 6979; ~2–4× OpenSSL nistz256; amd64 asm + portable fallback.** std's P-256 is portable pure-Zig (fiat Montgomery field, no asm, no endomorphism), several× slower than OpenSSL nistz256, and it sits on the **P2 HTTPS-API hot path** (per-request JWT ES256 verify), 2FA/WebAuthn (`ctap2pin`), `spake2plus`, and the P-256 suites of `hpke`/`voprf`/`mls`/`jwe`; p256 is our own speed-specialized P-256 keeping the zero-C/no-libc invariant. The base field `Fe` over `p = 2^256 − 2^224 + 2^192 + 2^96 − 1` uses the curve-specific **Solinas reduction** (`2^256 ≡ 2^224 − 2^192 − 2^96 + 1`) on four full 2^64 limbs — a portable wide-integer version is the correctness ORACLE (byte-exact vs `std.crypto.ecc.P256.Fe`), and on x86-64+ADX+BMI2 the irreducible core is a hand-written **`MULX/ADCX/ADOX` dual-carry-chain** 4×4→8-limb multiply + dedicated square followed by the signed **NIST word-shuffle** reduction (HMV Alg. 2.29: `s1+2s2+2s3+s4+s5−s6−s7−s8−s9`, signed 5-limb accumulator + two `M`-folds + a masked `sbb`+XOR-blend final reduce). The group is homogeneous-projective Renes–Costello–Batina complete formulas (same as std) over `Fe`, byte-exact vs std at the point level. P-256 has **NO endomorphism** (unlike k256/secp256k1), so the second core is a fixed-base **comb** for `k·G` (comptime projective table, signed-digit, `blackBox`-guarded masked CT gather, no online doublings) + a CT windowed variable-base; the PUBLIC verify path (`u1·G + u2·Q`) rides an **interleaved wNAF (Straus–Shamir)** double-scalar mult (vartime, public-only). Anchors: field/group differentials vs std on thousands of random + fold-boundary-edge inputs, the live core-vs-portable gated differentials (asm==portable, comb/windowed==CT ladder + std), the RFC 6979 ECDSA-P256/SHA-256 vectors (accepted by both p256 and std), an ECDSA differential against std's signer (both directions), and two positive controls (broken-Solinas-constant reduction + corrupted-comb-table gather) that stay RED-catching. Fault-injected (corrupted Solinas fold, dropped MULX/ADOX carry, corrupted comb entry) — each goes RED. Field mul/sq run on SECRET signing scalars: CT-checked in the ReleaseFast disassembly (zero secret-dependent Jcc/cmov, fixed-offset masked gather, `blackBox`-laundered negation confirmed leak-free); wNAF verify is vartime/public-only per contract. This host: field mul ~1.7× std, CT `k·G` ~7.5× std, sign ~3.6× / verify ~3.0× nistz256 | amd64 asm + portable fallback | — |
| `bip32` | **BIP-39 mnemonic seed phrases + BIP-32 hierarchical-deterministic (HD) keys over secp256k1** — the key-derivation foundation under every Bitcoin/Lightning wallet in this collection: BIP-39 `entropyToMnemonic`/`mnemonicToEntropy`/`validateMnemonic` (the embedded official 2048-word English wordlist, SHA-256 checksum bits folded into the 11-bit word groups) + `mnemonicToSeed` (PBKDF2-HMAC-SHA512, 2048 rounds, salt `"mnemonic"‖passphrase`), and BIP-32 `masterFromSeed` (`HMAC-SHA512(key="Bitcoin seed", seed)`), hardened + normal `ckdPriv`/`ckdPub` (child privkey `(IL+k_par) mod n` via `k256.Secp256k1.scalar`; child pubkey `IL·G + K_par` via `k256`'s comb `combMulBase` + point `.add`; both reject the spec's `IL≥n`/zero-result invalid-child case), `neuter`, xprv/xpub Base58Check `serializePriv`/`serializePub`/`parseExtended` (over `bech32.base58.checkEncode`/`checkDecode`, fingerprint via `ripemd160.hash160`), and `parsePath`/`derivePath` (`m/44'/0'/0'/0/0`-style paths). Byte-exact against the official **BIP-32 Test Vectors 1/2/3** (every chain-link xprv AND xpub, hardened + normal + leading-zero-byte retention) + **Test Vector 5**'s 13 invalid-key rejections, and the **BIP-39 Trezor test-vector corpus** (24 English vectors: entropy→mnemonic→seed→BIP-32-master-xprv, every step byte-exact) plus a checksum-tamper positive control. Secret-material buffers (`HMAC-SHA512` outputs, the hardened-derivation HMAC input, the PBKDF2 passphrase salt, decode scratch space) are `secureZero`'d before return; `ExtendedPrivKey.deinit` zeroes the long-lived struct | any | k256, ripemd160, bech32 |
| `blindrsa` | **RSA Blind Signatures (RFC 9474, RSABSSA)** over `rsa` — the anonymous-token / Privacy Pass primitive: `blind` (client blinds an EMSA-PSS-encoded message with a rejection-sampled invertible `r`), `blindSign` (server signs the blinded message with a §7.2-blinded CRT private op + a self-check), `finalize` (client unblinds → a standard RSA-PSS signature, fail-closed on the mandatory verify), and `verify`. Deterministic (PSSZERO) + randomized variants, SHA-384. The blinding-factor inverse runs a **masked** extended-Euclid (Euclid on `x·u` for a fresh mask `u`, no raw-secret timing leak); secrets `secureZero`'d. Byte-exact against RFC 9474 Appendix A.1 (randomized) and A.4 (PSSZERO deterministic) — `blinded_msg`, `inv`, `blind_sig`, and `sig` | any | rsa |
| `signal` | **Signal Protocol — X3DH (Part 1)** (signal.org/docs) — the Extended Triple Diffie-Hellman initial key agreement plus **XEdDSA** (signing with an X25519 key via a Montgomery→twisted-Edwards conversion — the one genuine std gap): `initiate`/`respond` (four X25519 DHs → HKDF shared secret + associated data, with/without a one-time prekey), the signed-prekey `xeddsa.sign`/`verify` including the missing `edwardsFromMontgomery` point recovery, and prekey-bundle / initial-message codecs. Const-time signer (branchless sign-0 forcing), fail-closed verify. Cross-checked byte-exact against libsignal's own Curve25519-signature vector, and a spec XEdDSA signature is confirmed to also verify under `std.crypto.sign.Ed25519` (shared challenge hash). NB: deployed libsignal smuggles the sign bit in `s`'s MSB — this ships the XEdDSA-**spec** variant (interop delta documented in SPEC). **Part 2** adds the **Double Ratchet** (`ratchet` — root / sending / receiving HKDF-HMAC key chains, an X25519 DH ratchet, out-of-order skipped-message keys with a `MAX_SKIP` DoS cap, ChaCha20-Poly1305 messages) seeded from the X3DH `SK`/`AD` — a usable Signal-style E2EE session with forward secrecy + post-compromise security. `decrypt` is **transactional** (advances ratchet state only after the AEAD tag verifies, so a forged message can't corrupt or DoS-grow the session); message keys are `secureZero`'d. Verified by self-consistency + spec-adherence (the spec ships no numeric KAT; libsignal/Olm wire-compat, header encryption, and PQXDH are out of scope) | any | — |
| `mls` | **MLS — Messaging Layer Security (RFC 9420) — Part 1: RFC 9420 foundation (cipher suite + crypto-basics + tree-math + codec) + Part 2: TreeKEM (ratchet tree, resolution, parent-hash, UpdatePath); proposals/framing/key-schedule later** — the scalable group-messaging complement to `signal`'s 1:1 sessions. The RFC 9420 §2.1 TLS-presentation-language codec (fixed-width ints, `optional<T>`, enums + RFC 9420's one deviation from RFC 8446: **QUIC-style variable-length-integer vector length prefixes**, §2.1.2 — minimal-encoding-enforced, truncation/OOB-safe), cipher suite `0x0001` (`MLS_128_DHKEMX25519_AES128GCM_SHA256_Ed25519`, the MTI suite) wired over the sibling `hpke` module, and the §5/§8/§9.1 labeled-crypto primitives (`RefHash`/`make_keypackage_ref`/`make_proposal_ref`, `ExpandWithLabel`/`DeriveSecret`/`DeriveTreeSecret`, `SignWithLabel`/`VerifyWithLabel`, `EncryptWithLabel`/`DecryptWithLabel` over HPKE `sealBase`/`openBase`) with the `"MLS 1.0 "`-prefixed length-framed wire encodings, plus RFC 9420 Appendix C's array-based ratchet-tree math (`left`/`right`/`parent`/`sibling`/`root`/`direct_path`/`copath`, a direct port of the RFC's own Python). Byte-exact against the official `mlswg/mls-implementations` `crypto-basics.json` (incl. deterministic Ed25519 re-sign reproducing the vector signature + HPKE decrypt-to-known-plaintext) + `tree-math.json` (every node's relationships across sizes 1–512). **Part 2 (TreeKEM)** adds the ratchet tree (`LeafNode`/`ParentNode`/`RatchetTree` §7.1/§7.2 wire codec + leaf-signature verification + §7.8 tree hash + §7.7/§12.1 Add/Update/Remove edits) and the five algorithmically-hard cores — `resolution` (§4.1), `parentHash`/`validateParentHashes` (§7.9 parent-hash chain), and the `UpdatePath` walk `processUpdatePath` (receiver-side path-secret derivation → `commit_secret = path_secret[n+1]`) / `applyUpdatePath` (public-side merge). Byte-exact against the official `tree-validation.json` (resolution + tree hash + leaf signatures for every node across 14 trees, `validateParentHashes` accepting all 14 and rejecting 275 single-byte parent-hash tampers), `tree-operations.json` (5 Add/Update/Remove vectors), and `treekem.json` (62 UpdatePaths × 328 receiver views reproducing `commit_secret` + path secrets, and 62 merges reproducing `tree_hash_after`). KeyPackage/LeafNode/Proposal/Commit framing, key schedule + secret tree are later parts | any | hpke |
| `ed448` | **Ed448 + X448 — the 448-bit "Goldilocks" curve** (RFC 8032 + RFC 7748), a curve family std lacks — `Fp448` (p = 2⁴⁴⁸−2²²⁴−1) with a hand-rolled 8×56-bit-limb Solinas/Goldilocks reduction (limb-aligned, no cross-limb shifting), the **X448** Montgomery-ladder DH, and **Ed448 / Ed448ph** EdDSA (edwards448, cofactor 4, SHAKE256 + the `dom4` domain prefix). Constant-time ladder / signing scalar-mul / field ops. Byte-exact against RFC 7748 §5.2 + §6.2 (X448 scalarmult + the Diffie-Hellman example) and RFC 8032 §7.4 + §7.5 (Ed448 blank / 1-octet / with-context / 11-octet + Ed448ph) — keygen, 114-byte signatures, verify + tamper/context-binding rejects. decaf448 / hash-to-curve / batch-verify out of scope | any | — |
| `decaf448` | **decaf448 prime-order group** (RFC 9496, "The ristretto255 and decaf448 Groups", §5) over `ed448`'s edwards448 curve — the 448-bit sibling to `std.crypto.ecc.Ristretto255`, eliminating edwards448's cofactor-4 pitfalls to give a clean prime-order group for protocols that assume one (threshold signing, VRFs, anonymous credentials). `Element` (add/sub/negate/scalarMul/equals, identity/generator) over `ed448.ed448.Point`, the RFC 9496 §5.1 constants (`D`/`ONE_MINUS_D`/`ONE_MINUS_TWO_D`/`SQRT_MINUS_D`/`INVSQRT_MINUS_D`, self-checked against their defining relations), the 56-byte scalar wire-width bridge to `ed448.scalar`'s 57-byte form, and the four field-math cores — `sqrtRatioM1` (§5.2's `p≡3 (mod 4)` form, no `sqrt(-1)` twist), `encode` (§5.3.2), `decode` (§5.3.1), and the inner `MAP` behind `oneWayMap` (§5.3.4). Constant-time cores (`Fe` CT ops + `ctSelect`/`ctAbs`); decode's public-validity rejects are the only branches. Byte-exact against the official RFC 9496 Appendix B vectors — encode of `[0]G..[15]G` (B.1), decode round-trip, the 21 invalid-encoding rejections (B.2: non-canonical / negative / non-square), and the 7 one-way-map vectors (B.3). Closes `ed448`'s deferred decaf448 flag | any | ed448 |
| `slhdsa` | SLH-DSA (FIPS 205, standardized SPHINCS+) post-quantum stateless hash-based signatures — **all twelve parameter sets** (SHA2 + SHAKE, 128/192/256 s/f) keygen/sign/verify (WOTS+ · XMSS · hypertree · FORS), each NIST-ACVP-KAT-verified; pre-hash variants reserved | any | — |
| `falcon` | **Full FN-DSA** — Falcon-512 **and Falcon-1024** (the NIST PQ lattice signature): **verification + signing + key generation + all key/signature codecs**, byte-exact vs the NIST Round-3 KATs for both parameter sets (degree-generic negacyclic NTT mod 12289, SHAKE256 hash-to-point, compressed-sig codec; signing = strict-float FFT + ffSampling + samplerZ; keygen = NTRUGen + NTRUSolve, seed→pk/sk byte-exact both sets) | any | — |
| `hqc` | **HQC (Hamming Quasi-Cyclic)** — the code-based KEM NIST selected March 2025 as a structurally-independent backup to lattice-based ML-KEM (this repo's first code-based PQ module; `std` ships only lattice PQ). **Parts 1-3 COMPLETE: HQC code-based KEM (ring/params/PRNG + RM/RS decoder + PKE/FO-KEM), byte-exact vs NIST KAT hqc-128/192/256.** The ambient ring `R = F2[X]/(X^n−1)` (bit-packed `Elem`, XOR-add, cyclic-convolution `mul`, Hamming weight, LSB-first codec, truncate), the three NIST parameter sets (hqc-128/192/256, spec v5.0.0 Table 5/6, byte sizes internally consistent), and the byte-exact SHAKE256 PRNG/XOF/seedexpander + fixed-weight samplers (I/G/H/J oracles; `Xof.getBytes` 8-byte stream-rounding quirk; unbiased rejection sampler for keygen x/y, biased Algorithm-5 sampler for encryption r1/r2/e) — `Xof`/`hashI`/`hashG`/`sampleVect`/`sampleFixedWeightRejection` numeric-KAT-pinned byte-exact against the reference v5.0.0 `intermediates_values` fixture. Part 2 adds the concatenated [n1n2,k] code over GF(2^8): RS + duplicated RM(1,7) encoders (byte-exact encode KAT from the compiled reference C) and the Fable-hard decoders — RS (constant-time Berlekamp-Massey + Gao-Mateer additive-FFT root-finding + Forney) and RM (expand + fast Hadamard + find_peaks), exact ports of the v5.0.0 reference, with decode-correctness tests across all three parameter sets. **Part 3** composes these into the usable KEM — the HQC-PKE keygen/encrypt/decrypt (`s = x + h·y`; `u = r1 + h·r2`, `v = truncate(encode(m) + s·r2 + e)`; `decode(v − truncate(u·y))`) and the Fujisaki-Okamoto implicit-rejection transform (`(K,θ) = G(H(ek),m,salt)`, constant-time re-encrypt-and-compare selecting `K` or the `J(H(ek),σ,ct)` reject value), byte-exact (pk/sk/ct/ss) against the official NIST `.rsp` KAT for hqc-128/192/256 — driven exactly as the reference `main_kat.c` seeds one continuing SHAKE256 DRBG stream; pure composition over Parts 1-2, no Fable | any | — |
| `dtls` | DTLS 1.3 (RFC 9147), **PSK mode** — the crypto core + record layer: the `"dtls13"` key schedule (RFC 8448-anchored byte-for-byte), AEAD record protection + §4.2.3 sequence-number encryption (OpenSSL-validated), unified/legacy record headers, handshake fragmentation/reassembly, §7 ACK + retransmission bookkeeping, §4.5.1 anti-replay window; certificate path + the full handshake flight engine deferred. Target consumer: CoAP-over-DTLS (RFC 7925) | any | rsa, x509 |
| `ocsp` | RFC 6960 OCSP — build an OCSPRequest and parse + **cryptographically verify** an OCSPResponse (authorized responders incl. delegated `id-kp-OCSPSigning`, RSA / ECDSA-P256 signature, CertID binding, `thisUpdate`/`nextUpdate` freshness, nonce) for TLS OCSP-stapling; defensive bounded DER, never calls `std.crypto.Certificate.parse` on untrusted input | any | x509, rsa, p256 |
| `ocspcache` | OCSP-stapling fetch + cache on top of `ocsp` — AIA responder-URL discovery, injectable-transport fetch (POST/GET), **verify-before-cache**, `nextUpdate`-derived expiry + refresh-ahead margin, soft-fail serving of the still-valid cached response when the responder is unreachable | any | ocsp, http, x509 |
| `tlsresume` | Server-side TLS 1.3 session-ticket resumption (RFC 8446 §4.2.11/§4.6.1/§7.1/§8) — `NewSessionTicket` codec + **AES-256-GCM STEK-ring** ticket seal/open (key-id-bound AAD, rotation-safe) + resumption-PSK/**binder** derivation (const-time verify) + ticket-age (de)obfuscation + single-use anti-replay register + **0-RTT early-data key schedule** (§4.2.10/§7.3: `c e traffic` / `e exp master` / early record key+iv), **byte-exact KAT vs RFC 8448 §3/§4** (incl. opening the trace's real encrypted early-data record). Engine-agnostic (owns no TLS state machine) | any | — |
| `quic-crypto` | RFC 9001 (Using TLS to Secure QUIC) crypto seam — Initial-secret derivation (§5.2), per-secret key/iv/hp (§5.1), AEAD packet protection (§5.3), header protection (§5.4), key update (§6); engine-agnostic (no transport state machine), std-only. KAT-validated byte-exact against the RFC 9001 App. A vectors, both through `std.crypto` directly (independent oracle) and through the public API | any | — |
| `bip340` | BIP340 Schnorr signatures over secp256k1 (Bitcoin Taproot's signature scheme) over `std.crypto.ecc.Secp256k1` — tagged hashing, x-only public keys (even-y `lift_x`), `sign` (constant-time nonce even-y select + mandatory self-verify), `verify` (`sG−eP` equation), `verifyBatch`, key/signature codecs; **byte-exact against all 19 official BIP340 test vectors** (8 sign + 10 negative-verify) | any | k256 |
| `taproot` | BIP341 Taproot key-path output-key tweaking — `"TapTweak"` tagged hash, `tweakPublicKey` (`Q = P + t·G`, x-only + parity) and `tweakSecretKey` (even-y-normalized `q = d + t mod n`, delegating normalization to `bip340`); byte-exact against all 7 official BIP341 wallet test vectors (tweaked pubkey + privkey + parity) + a tweaked-key sign→verify round-trip | any | bip340, k256 |
| `bitcointx` | **Bitcoin transaction (de)serialization + signature hashing** — CompactSize varints, legacy AND BIP144 segwit wire codecs (`deserialize`/`serialize`, zero-copy: scriptSig/scriptPubKey/witness bytes are borrowed slices into the caller's buffer, never duplicated) with `txid`/`wtxid`, and all three deployed sighash algorithms: legacy `SignatureHash()` (scriptCode substitution, ALL/NONE/SINGLE × ANYONECANPAY, incl. the historical SIGHASH_SINGLE `uint256(1)` bug), **BIP143** segwit-v0 (hashPrevouts/hashSequence/hashOutputs midstates + the amount-committing preimage), and **BIP341** taproot key-path (`SigMsg` over prevouts/amounts/scriptpubkeys/sequences/outputs via `bip340.taggedHash("TapSighash", …)`, all 7 hashType combinations incl. SIGHASH_DEFAULT). Byte-exact against a real mainnet block-170 legacy tx (txid), BIP143's own published Native-P2WPKH signed example (round-trip + txid + wtxid), Bitcoin Core's `sighash.json` reference fixture (7 vectors spanning every legacy hashType combination), BIP143's own worked examples (intermediates + preimage + sighash), and the official `bip-0341/wallet-test-vectors.json` `keyPathSpending` vectors (SigMsg + sighash, all 7 hashType cases); hostile-input teeth (truncated/oversized-count/non-minimal-CompactSize all typed errors, no panic/OOM). Tapscript (BIP342) and annex support are out of scope (structurally noted, not half-built — see SPEC.md) | any | bip340 |
| `psbt` | **BIP174 Partially Signed Bitcoin Transaction (PSBT) v0** — binary (de)serialization (magic + global/input/output key-value maps) built on `bitcointx` for the embedded unsigned tx / UTXO tx bodies, plus the **Combiner** role (union of key-value pairs, first-PSBT-wins on conflict, BIP174-example lexicographic key ordering). Known field types (`UNSIGNED_TX`, `XPUB`, `VERSION`, `NON_WITNESS_UTXO`, `WITNESS_UTXO`, `PARTIAL_SIG`, `SIGHASH_TYPE`, `REDEEM_SCRIPT`, `WITNESS_SCRIPT`, `BIP32_DERIVATION`, `FINAL_SCRIPTSIG`/`FINAL_SCRIPTWITNESS`) get structural validation at parse time (the BIP's "no key data"/pubkey-length/fixed-value-length rules — the security core: rejects a filled scriptSig on the unsigned tx, a witness-serialized unsigned tx, duplicate keys, and every excess-keydata/wrong-pubkey-length malformation); every unrecognized/proprietary key type is opaque byte-for-byte passthrough per BIP174's preservation requirement. Byte-exact against all 20 of BIP174's official invalid-PSBT vectors (rejected, 19 with a specific pinned error) + 8 of its 10 official valid-PSBT vectors (round-trip; the other 2 hit a `bitcointx`-inherited, documented 0-input wire-format ambiguity) + the BIP's own worked Combiner example. Signer/Input-Finalizer/Transaction-Extractor are structurally deferred — they need a Bitcoin Script interpreter this repo doesn't have yet (see SPEC.md) | any | bitcointx, bitcoinscript |
| `bitcoinscript` | **Bitcoin Script consensus interpreter (VM)** — the stack machine that unblocks PSBT finalization/extraction: full push/flow-control/stack/arithmetic/crypto opcode set (disabled opcodes fail closed as `DisabledOpcode`), `CHECKSIG(VERIFY)`/`CHECKMULTISIG(VERIFY)` (ECDSA via `k256`, Schnorr via `bip340`, sighash via `bitcointx`), `CHECKLOCKTIMEVERIFY`/`CHECKSEQUENCEVERIFY` (BIP65/112), CScriptNum minimal encoding, and the DoS consensus limits (script/push/stack size, op-count metered even in an untaken IF branch). `verifyScript` executes bare-P2PK/P2PKH/multisig, BIP16 P2SH (incl. P2SH-wrapped segwit v0), P2WPKH/P2WSH, and P2TR **key-path** with a full `SCRIPT_VERIFY_*` flag set. Result-class byte-exact against **296 rows of Bitcoin Core's official `script_tests.json`** (run through Core's exact synthetic credit/spend tx pair) + end-to-end real P2PKH/P2WPKH/P2TR self-signed spends (each with a tampered-signature reject control). Tapscript (BIP342 script-path / `OP_CHECKSIGADD`) is detected + rejected `TapscriptUnsupported`, structurally deferred | any | bitcointx, k256, bip340, ripemd160 |
| `btcp2p` | **Bitcoin P2P wire-message codec** — the message envelope (4-byte network magic for mainnet/testnet3/regtest/signet, 12-byte NUL-padded command, little-endian length, double-SHA256 checksum — the untrusted-input boundary, fail-closed on bad magic/checksum/oversized-length-claim before any downstream decoder runs), the `version`/`verack` handshake (`net_addr` peer addresses, nonce, user agent, start height, optional relay flag), inventory/data messages (`inv`/`getdata`/`notfound`, `getblocks`/`getheaders`, `headers`), and connection housekeeping (`ping`/`pong` BIP31 nonce form, `addr`/`getaddr`, `reject`/BIP61 flagged deprecated — disabled by Bitcoin Core since v0.18, removed in v0.20.0). `tx`/`block` payloads reuse `bitcointx.deserialize`/`serialize` directly (no reimplementation) — `block`'s transaction list rides `bitcointx.deserializePartial`'s documented back-to-back-decode contract. A codec only: no chain state, no peer manager, no block/script validation, no connection lifecycle (standing repo decision — wallet/signing code talks to `bitcoind`/Electrum/Esplora). Byte-exact against the Bitcoin wiki's own published hex dumps (`version`, `verack`, `addr`, `net_addr`) and the real, published genesis-block bytes (header + coinbase tx, block hash reproduced) | any | bitcointx |
| `musig2` | MuSig2 multi-signature (BIP327) producing BIP340 signatures — key aggregation (rogue-key-safe coefficients + "second key" rule), 2-round nonce gen/aggregation, partial sign/verify, partial-sig aggregation, with the security-critical parity/`g` bookkeeping routed through one shared helper (signer/verifier/aggregator can't disagree); plus BIP327 key tweaking (`applyTweak`: plain/BIP32 + x-only/Taproot, composing via the `gacc`/`tacc` accumulators); byte-exact against the official BIP327 key_agg/nonce_agg/sign_verify/sig_agg/tweak vectors + end-to-end multi-signer (untweaked AND TapTweak'd) → `bip340.verify` round-trips | any | bip340, k256 |
| `sphinx` | Lightning **BOLT#4 Sphinx onion routing** (the mix-net that gives Lightning payment privacy) — forward ECDH + blinding-factor chain, `rho`/`mu`/`um`/`pad` key derivation, `construct` (filler generation + reverse-order layered wrap) and `process` (constant-time HMAC gate → `timing_safe.eql`, deobfuscate, peel one layer, blind the next ephemeral); `construct` reproduces the official BOLT#4 1366-byte onion packet **byte-exact** + a 5-hop construct→process round-trip | any | k256 |
| `noise` | The generic **Noise Protocol Framework** (noiseprotocol.org, spec rev 34) — handshake patterns as data (NN/NK/XX/IK, §7) + a comptime-parameterized DH/AEAD/hash `Suite`; `CipherState`/`SymmetricState`/`HandshakeState` (§5) with the HKDF/HMAC ratchet. Deliberately distinct from `wireguard`'s hard-wired IKpsk2 instantiation; the foundation under `bolt8`, whose byte-exact BOLT#8 appendix vectors exercise it end-to-end | any | — |
| `bolt8` | Lightning **BOLT#8 encrypted transport** (`Noise_XK_secp256k1_ChaChaPoly_SHA256`) — the `act1`/`act2`/`act3` handshake (driven over `noise`'s `SymmetricState` with a secp256k1 DH adapter, `ECDH = SHA256(compressed(k·P))`) + the post-handshake transport with 1000-message key rotation; act1/2/3 **byte-exact** against BOLT#8 Appendix A + a full initiator↔responder handshake yielding the published transport keys + a message-0 round-trip | any | noise, k256 |
| `bolt3` | Lightning **BOLT#3 key derivation** — the commitment scheme's secp256k1 crypto pocket: per-commitment blinded keys (`basepoint + SHA256(pcp ‖ basepoint)·G`, public + secret), the split-secret revocation keys (the justice-transaction primitive neither party alone can complete), and Appendix D shachain per-commitment secret generation; byte-exact against the BOLT#3 Appendix D + E vectors | any | k256 |
| `lnwire` | Lightning **BOLT#1/2/7 wire messages** (the message codec that rides on `bolt8`'s encrypted transport) — the BOLT#1 base frame (2-byte type + payload), the **BigSize** varint + the **TLV stream** with the full canonical rules (strictly-increasing types, minimal-encoding, unknown-even-type = required-so-reject, truncation/overflow fail-closed); BOLT#2 channel messages (`open`/`accept_channel`, `funding_created`/`signed`, `channel_ready`, `update_add`/`fulfill`/`fail_htlc`, `commitment_signed`, `revoke_and_ack`, `update_fee`, `shutdown`, `closing_signed` + `init`/`error`/`ping`/`pong`); BOLT#7 gossip (`channel_announcement`, `node_announcement`, `channel_update`, query/reply range + short-channel-ids) plus the double-SHA256 announcement-signing digest helpers (verification is the caller's secp256k1). Byte-exact against BOLT#1 Appendix A (BigSize: 8 valid + non-canonical + truncation) + Appendix B (TLV valid/invalid) vectors; every message has truncation/oversized-length-prefix teeth (`error.Truncated`, no OOB/OOM). BOLT#11/#12 invoices/offers + signature verification deferred (see SPEC.md) | any | — |
| `lninvoice` | Lightning **BOLT#11 payment requests** (+ BOLT#12 offer decode) — decode+verify and encode+sign of a `ln`-prefixed invoice: hrp (currency + amount multiplier m/u/n/p), 35-bit timestamp, all tagged fields (`p`/`s`/`d`/`h`/`x`/`c`/`n`/`f`/`r`/`9`), and the 65-byte **recoverable** ECDSA signature — **RFC 6979 deterministic ECDSA sign + standard public-key recovery** (`Q = r⁻¹(sR − eG)`) live in `k256.ecdsa_recover` (moved there from this module once it turned out to be general secp256k1 machinery, not BOLT#11-specific); this module re-exports it at root (`sign`/`recoverPubkey`/`Signature`). The security core is signer recovery: `decode` recovers the node pubkey from the signature and checks it against the `n` field (or exposes it when `n` is absent), so a tampered invoice recovers a wrong/failing key. Byte-exact against BOLT#11's own worked examples (donation/coffee/pico-amount/description-hash/uppercase invoices, node id `03e7156a…` recovered) with bad-checksum/mixed-case/no-separator/high-S/unknown-required-field reject controls; found + documented two real spec subtleties (the high-S vector's inconsistent recovery-id, cross-checked vs Python; `must-be-ignored` = first *correctly-sized* field wins). BOLT#12 = offer TLV decode via `lnwire` + Merkle-tree BIP-340 signing (`bip340.taggedHashRuntime`/`xonlyBytesOf`); invoice_request/invoice deferred fields noted in SPEC.md | any | bech32, k256, lnwire, bip340 |
| `hpke` | **HPKE — Hybrid Public Key Encryption (RFC 9180)** — DHKEM(X25519 + P-256) Encap/Decap, the LabeledExtract/LabeledExpand key schedule (base/PSK/auth/auth-PSK modes), AEAD `seal`/`open` (seq-nonce, fail-closed overflow) + `export`, single-shot `sealBase`/`sealPsk`/`sealAuth`/`sealAuthPsk` + `open*` mirrors; byte-exact against RFC 9180 Appendix A in **all four modes** — A.1.1/A.1.2/A.1.3/A.1.4 (X25519/AES-128-GCM), A.2 (ChaCha20Poly1305), A.3.1–A.3.4 (P-256), including the `AuthEncap`/`AuthDecap` fold and `DeriveKeyPair` for both KEMs | any | p256 |
| `adaptor` | **Schnorr adaptor signatures** (scriptless scripts / the crypto behind Lightning **PTLCs** + atomic swaps) over BIP340 — `preSign`/`preVerify`/`adapt`/`extract`: a pre-signature encrypted under an adaptor point `T = t·G`, which `adapt` completes into a plain BIP340 signature once `t` is known and `extract` turns back into `t` once that signature is public (the deliberate one-time key-leak). Const-time nonce parity + the non-recomputable `needs_negation` bit handled per secp256kfun; `adapt`'s output verifies under plain `bip340.verify` (chaining to BIP340's 19 official vectors); 6 self-authored byte-exact vectors (independent Python secp256k1 reference, both parity + both key-normalization branches) + full round-trip/tamper harness | any | bip340, k256 |
| `frost` | **FROST — Flexible Round-Optimized Schnorr Threshold signatures (RFC 9591)**, secp256k1/SHA-256 ciphersuite — a t-of-n threshold Schnorr scheme: trusted-dealer keygen (Shamir + Feldman VSS), 2-round signing (`round1Commit` nonces/commitments → `round2Sign` share), `aggregate` into one Schnorr signature, `verifySignatureShare` + `verify` (RFC 9591 App. B `prime_order_verify`). Lagrange interpolation via the scalar field's const-time modular inverse; binding factors (RFC 9380 `expand_message_xmd`, hash layer validated vs App. E.5 intermediates); secret-touching arithmetic const-time; `verify` guards signature `z` non-canonicality (malleability). Byte-exact against RFC 9591 Appendix E.5 (shares, binding factors, both signature shares, the aggregate signature) + a (2,3) end-to-end round-trip. NB: FROST-secp256k1's own Schnorr (33-byte compressed R, `"chal"`-DST challenge) — NOT BIP340-compatible | any | bip340, k256 |
| `oscore` | **OSCORE — Object Security for Constrained RESTful Environments (RFC 8613)** — end-to-end object security for CoAP: §3.2.1 HKDF-SHA-256 security-context derivation (`deriveKey`/`deriveContext` → Sender/Recipient keys + Common IV), §5.2 AEAD nonce construction, §5.4 COSE `Enc_structure` AAD, §8 `protect`/`unprotect` over **AES-CCM-16-64-128** (std's `Aes128Ccm8` IS exactly this suite), and the §3.2.2 anti-replay sliding window (checked before decrypt, updated only after the AEAD verifies — fail-closed). Byte-exact against RFC 8613 Appendix C (C.1-C.3 key derivation, C.4-C.8 protected request/response incl. the request-nonce-reuse response) + tamper/replay rejection + end-to-end round-trip. Intended consumer: the sibling `coap` module (crypto core kept coap-agnostic) | any | — |
| `spake2plus` | **SPAKE2+ — an augmented (asymmetric) PAKE (RFC 9383)**, P-256/SHA-256/HKDF/HMAC ciphersuite (the Matter/Thread commissioning PAKE) — `proverStart`/`verifierStart` (X = x·P + w0·M, Y = y·P + w0·N over the fixed §4 M/N points), `proverFinish`/`verifierFinish` (the augmented Z/V terms — Prover uses `w1`, Verifier uses `L = w1·P`, the SPAKE2+ asymmetry that resists server-compromise), the §3.4 length-prefixed transcript → K_main → HKDF confirm/shared keys, and constant-time confirmation-MAC verification (fail-closed). Secret scalar-muls use const-time `P256.mul`. Byte-exact against RFC 9383 Appendix C (L, shareP/shareV, Z, V, the 570-byte transcript, K_main, both confirm keys, confirmP/confirmV, K_shared) + tamper rejection + end-to-end round-trip | any | p256 |
| `voprf` | **(V)OPRF — Oblivious Pseudorandom Functions (RFC 9497)**, ristretto255-SHA-512 ciphersuite, all three modes: **OPRF** (base), **VOPRF** (verifiable) + **POPRF** (partially-oblivious) — `blind`/`blindEvaluate`/`finalize`, `deriveKeyPair`, and the §2.2 DLEQ `generateProof`/`verifyProof` (Chaum–Pedersen `s = r − c·k`; verify recomputes composites without the key, `timing_safe` challenge compare, fail-closed `InvalidProof`). `hash_to_ristretto255` built on std's `Ristretto255.fromUniform` (the Elligator half) + an in-module `expand_message_xmd(SHA-512)` (validated vs RFC 9380 Appendix K.3). Byte-exact against RFC 9497 Appendix A.1 for OPRF+VOPRF+POPRF (skS/pkS, BlindedElement, EvaluationElement, Proof incl. the batch-size-2 DLEQ vectors, Output) + tamper rejection + end-to-end round-trips | any | — |
| `bulletproofs` | **Bulletproofs — zero-knowledge range proofs over Ristretto255** (Bünz/Bootle/Boneh/Poelstra/Wuille/Maxwell, IEEE S&P 2018, eprint 2017/1066) — prove a Pedersen-committed value `v ∈ [0, 2^n)` without revealing `v`, in a proof **logarithmic** in `n` (the construction behind Confidential Transactions / Monero range proofs / many zk-rollups). Ships the log-size **Inner-Product Argument** (`proveIpa`/`verifyIpa` — the `log2(n)`-round recursive vector fold + single multi-exponentiation verify check `Σ(a·sᵢ)Gᵢ + Σ(b·sᵢ⁻¹)Hᵢ + (a·b)Q == P + Σ(uⱼ²Lⱼ + uⱼ⁻²Rⱼ)`) and the **range proof** on top (`prove`/`verify` — bit-decomposition `a_L`/`a_R`, `A`/`S`/`T1`/`T2` commitments, the `y`/`z`/`x`/`w` Fiat-Shamir challenge chain, `l(x)`/`r(x)`/`t(x)` polynomials → `tau_x`/`mu`/`t_hat`, reduced to one IPA over the `t_hat` relation `t_hat·G + tau_x·H == z²·V + δ(y,z)·G + x·T1 + x²·T2` plus the reconstructed IPA point), `commit` (Pedersen value commitment), `deltaYZ`, both proof structs' byte codecs, and a deterministic NUMS generator derivation. Self-contained **SHA-512 Fiat-Shamir transcript** (module-defined, NOT dalek/Merlin-compatible — so **property/soundness-verified, not byte-exact** against any third-party vector): completeness (incl. `v=0` and the `2^n-1` boundary + standalone IPA) + an exhaustive per-field tamper suite (`A`/`S`/`T1`/`T2`/`tau_x`/`mu`/`t_hat`, every IPA `Lᵢ`/`Rᵢ`, final `a`/`b`) + cross-commitment + mismatched-`n` rejection, all fail-closed. **Caveats:** `prove` is Linux-only (internal `getrandom(2)` blinding — verify/codecs are portable); `multiScalarMul` is not constant-time (mild prover-side timing side-channel — ZK privacy unaffected). Single-value (`m=1`); `n≤64` | linux | — |
| `ctap2pin` | **CTAP2 `pinUvAuthProtocol` (FIDO2 / WebAuthn CTAP 2.1)** — both protocol One & Two: ECDH-P256 key agreement (`encapsulate` → shared secret), `encrypt`/`decrypt`, `authenticate`/`verify`. Protocol One = `SHA-256(Z)` key, zero-IV AES-256-CBC, 16-byte HMAC-SHA-256 tag; Protocol Two = HKDF-SHA-256 (`"CTAP2 HMAC key"`/`"CTAP2 AES key"`) → separate MAC+AES keys, random-IV AES-256-CBC (`IV‖ct`), full 32-byte tag. **Ships AES-256-CBC** — a genuine Zig-std gap (std has GCM/CCM/OCB AEADs + the raw block cipher, no CBC mode) — byte-exact against **NIST SP 800-38A** F.2.5/F.2.6; ECDH-P256 vs RFC 5903, HKDF vs RFC 5869, HMAC vs RFC 4231; MAC compare `timing_safe`, fail-closed. Caller-supplied randomness (scalar/IV) → reproducible KATs. Consumer: FIDO2/passkey authenticators | any | p256 |
| `webauthn` | **WebAuthn / FIDO2 Relying-Party VERIFIER (W3C WebAuthn Level 3)** — `verifyAssertion` (authentication ceremony, §7.2): clientDataJSON type/challenge/origin, `rpIdHash`, User Present/Verified flags, and the signature over `authData‖SHA-256(clientDataJSON)` under the stored credential's COSE key — **ES256/EdDSA/RS256**, dispatched off the sibling `p256`/`std.crypto.sign.Ed25519`/`rsa` primitives (this module hand-rolls no crypto, only spec-assembly). `verifyAttestation` (registration ceremony, §7.1/§8): parses the CBOR attestationObject, extracts the attested credential's COSE public key (extends `cbor.cose` with the RFC 8230 RSA `kty=3` shape it deliberately omits), and verifies **`none`**, **`packed`** (self-attestation + x5c/basic, dispatched on the leaf cert's own EC/Ed25519/RSA key via `std.crypto.Certificate.parse`), and **`fido-u2f`** (the `0x00‖rpIdHash‖clientDataHash‖credentialId‖publicKeyU2F` U2F-era signed blob). `tpm`/`android-key` are structurally recognized and rejected `error.UnsupportedFormat` — DEFERRED (manufacturer cert-chain formats, a project of their own), never half-verified. **External byte-exact anchor:** the W3C WebAuthn Level 3 spec's own §16 conformance test vectors (fetched directly from w3.org, cross-checked against an independent re-hosting), covering all three assertion algorithms + all three implemented attestation formats — plus a full adversarial reject-teeth suite (tampered authData/signature/clientDataHash byte, wrong rpId/challenge/origin/type, UP/UV flag clear, cross-algorithm and cross-credential key swap) proving every check is load-bearing. Backup-code/recovery flows are a product concern layered above WebAuthn, out of scope | any | cbor, rsa, p256 |
| `otp` | **HOTP + TOTP one-time passwords (RFC 4226 / RFC 6238)** — the 2FA-authenticator primitive: `hotp` (HMAC-SHA-1/256/512 of an 8-byte counter → dynamic truncation → D-digit code), `totp` (HOTP over `floor((t−T0)/period)`), `totpVerify` (± skew window, whole-window eval, no early-exit), plus zero-padded formatting. Byte-exact against RFC 4226 Appendix D (all 10 counts incl. the HMAC + truncation intermediates) and RFC 6238 Appendix B (all 6 times × SHA-1/256/512). Caller-supplied counter/time (no wall-clock) | any | — |
| `xmss` | **XMSS — eXtended Merkle Signature Scheme (RFC 8391)**, single-tree, SHA-256 suite (`XmssSha2_10/16/20_256`) — a **stateful** hash-based signature: WOTS+ one-time sigs (chain/base-w/checksum), the L-tree + `treeHash` + authentication path, `keyGen`/`sign`/`verify`, and the full ADRS-keyed F/H/H_msg/PRF layer. `sign` advances the one-time index **before** producing the signature (crash-safe against index reuse) and returns `KeyExhausted` after 2^h uses (index reuse breaks the scheme — see the doc-comment hazard note). Byte-exact against the official `XMSS/xmss-reference` implementation: the hash primitives, full WOTS+ (pkGen/sign/pkFromSig), h=4 keygen+sign, and h=10 `verify` of real external (pk,msg,sig) triples + 10 tamper-rejection variants; plus a stateful keygen→sign→verify walk. XMSS^MT / SHAKE suites / BDS traversal out of scope | any | — |
| `bls12_381` | **BLS12-381 pairing-friendly curve — Part 1: field tower + groups** (foundation for BLS signatures / KZG polynomial commitments / threshold-BLS) — the base field `Fp` and its `Fp2`/`Fp6`/`Fp12` extension tower + scalar field `Fr` over `std.crypto.ff` constant-time Montgomery arithmetic, and the groups `G1` (`y²=x³+4` / Fp) + `G2` (`y²=x³+4(1+u)` / Fp2) in Jacobian coordinates: complete branchless point add/double, double-and-add-always `scalarMul`, `isOnCurve`, `[r]P==O` subgroup check + cofactor clearing, and ZCash/IETF compressed/uncompressed (de)serialization. Frobenius coefficients derived programmatically (`frobenius(a)==a^p` pinned, no transcribed table); `Fp2.sqrt` via Adj–Rodríguez-Henríquez. Byte-exact against the canonical G1/G2 generator serializations (compressed `97f1d3a7…`) + independent affine-reference `[k]G` cross-checks + a full group-law / field-identity suite; a scaffold `h2` cofactor error (the order of E(Fp2) vs. the sextic twist E') was caught by a Lagrange check and corrected. **Part 2** adds the optimal-ate pairing `e: G1×G2→Gt` (Miller loop over the BLS seed + Hayashida–Hayasaka–Teruya exact-`d` final exponentiation), `cyclotomicSquare`, and `multiMillerLoop`/`pairingCheck` (product-of-pairings == 1 for BLS-verify / KZG) — byte-exact against the IETF `pairing-friendly-curves` draft's official optimal-ate vector (cross-checked vs `py_ecc`), plus a full bilinearity suite; a subtly-wrong `f^(3d)` final-exp chain was rejected by the byte-exact KAT. **Part 3** adds RFC 9380 hash-to-curve (`hashToCurveG1`/`G2`, the `XMD:SHA-256_SSWU_RO_` suites — `expand_message_xmd` + Simplified-SWU map + 11-isogeny/3-isogeny + cofactor clear), byte-exact against RFC 9380 Appendix J.9.1/J.10.1 (u/Q0/Q1 intermediates + final P). **Part 4** adds BLS signatures (draft-irtf-cfrg-bls-signature, min-pk / ProofOfPossession) — `sign`/`verify`/`aggregate`/`aggregateVerify`/`fastAggregateVerify` + proof-of-possession over the `e(PK,H(m))==e(G1,sig)` pairing check, with mandatory fail-closed KeyValidate + signature-subgroup checks (rogue-key / small-subgroup defence); byte-exact against the `ethereum/bls12-381-tests` vectors. **Part 5** adds EIP-4844 KZG polynomial commitments (`blobToKzgCommitment` / `computeKzgProof` / `verifyKzgProof` + blob-proof + `verifyBlobKzgProofBatch`) over the embedded official KZG-ceremony trusted setup — Fr NTT + roots-of-unity, Pippenger multi-scalar mult, barycentric evaluation-form opening, and the `e(π,[s−z]₂)==e(C−[y]₁,[1]₂)` verify pairing; byte-exact against the `ethereum/c-kzg-4844` test vectors. **Part 6** adds trusted-dealer threshold BLS (`splitSecretKey`/`partialSign`/`combineSignatures`) — Shamir sharing + Feldman VSS + Lagrange-in-exponent combine, where a `t`-of-`n` combined signature equals the single-key BLS signature byte-for-byte and verifies under the group public key (chaining threshold to Part 4's pinned vectors) | any | — |
| `bbs` | **BBS selective-disclosure signatures over `bls12_381`** (draft-irtf-cfrg-bbs-signatures-04, `BBS_BLS12381G1_XMD:SHA-256_SSWU_RO_` ciphersuite) — the pairing-based multi-message signature scheme behind anonymous / verifiable credentials (W3C VC selective disclosure, mDL): sign a vector of messages under one header with a single constant-size 80-byte signature, then later prove knowledge of that signature while revealing only a chosen SUBSET of the messages, in zero knowledge (the signature and undisclosed messages stay hidden). `sign`/`verify` (CoreSign/CoreVerify — `e=hash_to_scalar(SK‖domain‖msgs)`, `B=P1+[domain]Q1+Σ[msgᵢ]Hᵢ`, `A=[(SK+e)⁻¹]B`, verified by the `e(A,W+[e]BP2)==e(B,BP2)` pairing check) plus the genuinely hard core: the draft-04 selective-disclosure **Fiat-Shamir NIZK** `proofGen`/`proofVerify` (3+U blinding scalars `r1,r2,r3,m̃ⱼ`; `Abar=[r1]A`, `Bbar=[r1]B−[e]Abar`, `T=[r2]Abar+[r3]Bbar+Σ[m̃ⱼ]Hⱼ`; challenge over the `Abar‖Bbar‖T‖R‖indexes‖disclosed-scalars‖domain‖ph` transcript; responses `r4=−r1⁻¹`, `r̂2=r2+e·r4·c`, `r̂3=r3+r4·c`, `m̂ⱼ=m̃ⱼ+msgⱼ·c`; verified by recomputing the challenge from `D`/`T` and the `e(Abar,W)==e(Bbar,BP2)` pairing check). Randomness is an explicit `[]const Fr` parameter (frost-style), so the draft's own deterministic mocked RNG and real entropy share one path. **Byte-exact against the official IETF draft-04 fixtures** (`mattrglobal/pairing_crypto`) — the exact signature bytes for `signature001`/`signature004`, the exact selective-disclosure proof bytes for `proof001`/`proof003` (fed the draft's mocked-RNG), plus signature/proof tamper-rejection (`signature002`/`proof004`); malformed input + duplicate/out-of-range disclosed indexes fail closed. Builds entirely on `bls12_381` (field/group/pairing/hash-to-curve); adds no new curve math. SHAKE-256 ciphersuite + blind-signature `commitment` extension out of scope | any | bls12_381 |
| `coconut` | **Coconut threshold-issuance selective-disclosure anonymous credentials over `bls12_381`** (Sonnino-Bano-Al-Bassam-Danezis, NDSS 2019 — the credential layer of Nym) — **Phase 1: COMPLETE (Fable core implemented).** `n` authorities each hold a Shamir share of a Pointcheval-Sanders (PS) secret key `(x, y₁…y_q)`; any `t` independently issue partial credentials on an attribute vector, which the user aggregates Lagrange-**in-exponent** into one short re-randomizable PS credential `σ=(h, h^{x+Σmᵢyᵢ})`, then SHOWS while revealing only a chosen subset of attributes in zero knowledge (credential + undisclosed attributes stay hidden; repeated shows unlinkable). **REAL + tested today:** Setup + attribute generators + the common base `h=hashToCurveG1(cm)` (`params.zig`); trusted-dealer threshold keygen + `vk=(g2^x, g2^{yᵢ})` derivation + Lagrange-in-exponent `vk` aggregation (`keys.zig`); the `Fr` Lagrange coefficient (`lagrange.zig`); wire codecs; and the mechanical PS oracles `psSignWithSecret`/`psVerifyPlain` (the `e(h,α·Πβᵢ^{mᵢ})==e(s,g2)` pairing verify with `h≠1` guard). **The Fable core (`gate.fable_core_implemented`, now `true` — four functions under one flag):** `signPartial` (share-keyed partial PS sig), `aggregateCredential` (Lagrange-in-exponent recombination), and the genuinely-hard `proveCredential`/`verifyCredential` (re-randomise σ, build κ/ν, the Fiat-Shamir selective-disclosure NIZK over hidden attributes + blinding, with the full transcript binding Aw/Bw + every disclosed index/value — SPEC §1a). **Tier finding: genuine Fable** — NO public byte-exact vector exists (the `asonnino/coconut` Python + `nymtech/coconut` Rust/Go refs publish only randomized round-trip tests; no IETF ciphersuite), so self-consistent tests can pass-while-unsound; the weight is in the NIZK (like `bbs`' `proofGen`/`proofVerify`), and Coconut is *more* Fable than `bbs` which at least had IETF mocked-RNG fixtures. **Teeth before core:** a `BrokenCoconut` positive control mechanically mis-aggregates partials (all-ones weights vs Lagrange) and `psVerifyPlain` REJECTS the mis-aggregation while ACCEPTING the Lagrange one (byte-matching the single-signer oracle); the end-to-end anchor (issue→aggregate→show→verify) PASSES + soundness rejections (wrong disclosed value / mutated challenge / too-few partials / forged undisclosed attribute) all REJECT. Blind-issuance ElGamal + dealer-free DKG wiring + SHAKE ciphersuite are deferred increments | any | bls12_381 |
| `tlock` | **drand-style timelock encryption (Boneh-Franklin IBE over `bls12_381`)** — encrypt to a FUTURE drand beacon round; decryption becomes possible only once the beacon publishes that round's threshold-BLS signature (which IS the BF-IBE private key for the round's identity, by construction). Models `drand/tlock` (Go) + `drand/kyber`'s `encrypt/ibe`, targeting the League of Entropy **quicknet** beacon (`SigsOnG1ID`/`"bls-unchained-g1-rfc9380"`). Ships the drand ciphersuite (`beaconId` unchained digest, `h1` identity→`G1` RFC-9380 hash-to-curve, `h2`/`h3`/`h4` domain-separated SHA-256 constructions), the 128-byte `(U∈G2, V, W)` `Ciphertext` wire codec (byte-for-byte drand's own), and the two Fable cores — `encrypt` (BF-IBE FullIdent: `Gid=e(Qid,P_pub)`, `r=H3(σ,M)`, `U=r·G2gen`, `V=σ⊕H2(Gid^r)`, `W=M⊕H4(σ)`, with a module-local constant-time `fp12Pow` for the `Gt` exponentiation) and `decrypt` (`Gid^r=e(σ_round,U)` by bilinearity + the mandatory Fiat-Shamir-Okamoto `U==r·G2gen` CCA consistency check, `error.FoCheckFailed` never a garbage plaintext). **Byte-exact interop with drand's Go `tle` IN BOTH DIRECTIONS** (decrypt a genuine committed `.tle` ciphertext to the exact age filekey; re-encrypt its recovered `(filekey, σ)` back to the identical 128 Go bytes) — this vector caught a real Gt-representation divergence a self-consistent round trip never could: drand/kilic's pairing VALUE is this repo's canonical optimal-ate value's CUBE (FCKRH `f^(3d)` final-exp vs `bls12_381`'s exact-`d`; both bilinear, `gcd(3,r)=1`), fixed by a private `gtToDrandRepr(gt)=gt³` applied before every H2 on both sides (`bls12_381` untouched). Plus ungated pairing-sanity KATs against live quicknet production data. **NOT post-quantum** (pairing-based — gates on TIME, not a PQ hardness assumption; hybridize with the sibling `hqc` KEM for long-term confidentiality). Crypto core of the S5 dead-man-switch | any | bls12_381 |
| `drand` | drand randomness-beacon client core — chain-info + round codec + BLS-verify a round signature against the chain public key (`bls12_381`, quicknet/unchained-G1 ciphersuite reused from `tlock`); transport-agnostic | any | bls12_381, tlock |
| `timelock_envelope` | Hybrid sealed envelope — plaintext unlocks only when BOTH a drand timelock round publishes (`tlock` time gate) AND the recipient holds the PQ-KEM secret (`hqc` long-term confidentiality); AEAD-sealed content. Crypto core of the S5 dead-man-switch | any | tlock, hqc, chachapoly |
| `ibe` | **Standalone Boneh-Franklin Identity-Based Encryption (BF-IBE "FullIdent", CRYPTO 2001 §4.2) over `bls12_381`** — the general case of the sibling `tlock` (where YOU run the PKG instead of a drand beacon): `setup` generates a master keypair (`msk∈Fr`, `mpk=msk·G2gen`), `extract(msk,id)` derives the private key `d_id=msk·H1(id)∈G1` for any identity string (email / device serial / policy tag), and `encrypt`/`decrypt` are the BF-IBE FullIdent core (`Gid=e(H1(id),mpk)`, `r=H3(σ,M)`, `U=r·G2gen`, `V=σ⊕H2(Gid^r)`, `W=M⊕H4(σ)` — with a constant-time `fp12Pow` `Gt` exponentiation — recovered on decrypt via `Gid^r=e(d_id,U)` bilinearity + the mandatory Fiat-Shamir-Okamoto `U==r·G2gen` CCA check, `error.FoCheckFailed` never a garbage plaintext). Reuses the exact BF-IBE assembly `tlock` proved byte-exact against drand's Go `tle`, but is a self-contained scheme with its own DSTs and **no drand-style Gt cube** (`tlock`'s `gt→gt³` adapter exists only to match kilic/bls12-381's final-exponentiation convention; `ibe` has no external interop target, so `encrypt`/`decrypt` both feed `H2` the canonical `bls12_381.pairing` value and correctness needs only internal consistency). Self-consistency + soundness KATs (round-trip across several identities; the core `e(d_id,U)==Gid^r` and Extract `e(d_id,G2gen)==e(H1(id),mpk)` identities checked directly; tampered U/V/W, wrong-key, cross-identity, and cross-PKG ciphertexts all rejected) — meaningful because every primitive is already byte-exact-grounded via `bls12_381` + `tlock`'s drand interop. **NOT post-quantum** (pairing-based; hybridize with the `hqc` KEM for long-term confidentiality). Key escrow inherent by design (the PKG can extract any identity's key) | any | bls12_381 |
| `bn254` | **BN254 / alt-bn128 curve — Parts 1-6 COMPLETE: field tower + G1/G2 + optimal-ate pairing + EIP-196/197 precompiles + Groth16 verifier** (foundation for the Ethereum EIP-196/197 pairing precompiles + a Groth16 verifier) — the base field `Fp` and its `Fp2`/`Fp6`/`Fp12` extension tower + scalar field `Fr` over `std.crypto.ff` constant-time Montgomery arithmetic. `Fp2=Fp[u]/(u²+1)`, `Fp6=Fp2[v]/(v³−ξ)` with the standard BN254 non-residue `ξ=9+u`, `Fp12=Fp6[w]/(w²−v)`; tower mul/square/inv + `Fp2.sqrt` (Adj–Rodríguez-Henríquez) + Granger–Scott `cyclotomicSquare` adapted unchanged from `bls12_381` (generic in the non-residue). Frobenius coefficients derived programmatically (`frobenius(a)==a^p` pinned, no transcribed table). `p`/`r`/`ξ` independently re-derived from the BN polynomial family + cross-checked vs EIP-196/197 and `py_ecc`; G1 generator `(1,2)` pinned on `y²=x³+3`, and byte-exact KAT vectors (from a from-scratch big-int reference) for Fp2/Fp6/Fp12 mul/inv + the Fp6/Fp12 Frobenius coefficients. **Part 3** adds `G1`/`G2` Jacobian group arithmetic (constant-time `scalarMul`), on-curve + `[r]P==O` subgroup checks (G1 cofactor 1; G2 twist `b'=3/(9+u)` derived, with a negative on-twist-non-subgroup test), and EIP-196/197 uncompressed codecs (imaginary-first `Fp2` ordering for G2), all pinned to byte-exact `[k]G` py_ecc cross-checks. **Part 4** adds the optimal-ate pairing — the `6x+2` Miller loop with the BN-specific Frobenius tail + the BN exact-`d` final exponentiation (Scott et al. 2008/490), `e`/`multiMillerLoop`/`pairingCheck` (EIP-197), byte-exact vs py_ecc bn128. **Part 5** adds the EIP-196/197 EVM precompile entry points `ecAdd`/`ecMul`/`ecPairing` (`0x06`/`0x07`/`0x08`) — pure ABI composition over Parts 1-4 (calldata padding/truncation, point decode, group op, re-encode) with the mandatory G2-subgroup check in `ecPairing`, byte-exact against the official `ethereum/go-ethereum` precompile conformance vectors (true + false pairing cases). **Part 6** adds a Groth16 zkSNARK verifier — the public-input MSM commitment `vk_x = IC[0]+Σ pubᵢ·ICᵢ` (G1) plus the single-product pairing check `e(−A,B)·e(α,β)·e(vk_x,γ)·e(C,δ)==1` (the snarkjs/Ethereum form) over Parts 1-5, with the untrusted proof's `A`/`C` on-curve + `B` `[r]P==O` subgroup checks enforced unconditionally (soundness-critical); byte-exact against a REAL Dark Forest v0.3 snarkjs proof (sign convention independently confirmed via `py_ecc`), with 5 independent tamper variants rejected | any | — |
| `opaque` | **OPAQUE — an asymmetric PAKE (RFC 9807)**, ristretto255-SHA-512 + **3DH** + internal envelope (Identity KSF) — the offline registration (`createRegistrationRequest`/`Response`/`finalize` → the `RegistrationRecord`) and the online login/AKE (`generateKE1`/`KE2`/`KE3` + `serverFinish`): OPRF-blinded password (reusing the sibling `voprf` OPRF), the `randomized_password`-derived envelope, `masking_key`-XOR credential response, the triple-DH key schedule → `session_key`/`export_key`, and both transcript MACs verified `timing_safe` + fail-closed (`ServerAuthentication`/`ClientAuthentication`). Secrets `secureZero`'d. A server compromise reveals no password (aPAKE). Byte-exact against RFC 9807 Appendix C.1 (registration record + KE1/KE2/KE3 + session_key + export_key) + tamper rejection + fresh end-to-end round-trip | any | voprf |
| `ecvrf` | **ECVRF-EDWARDS25519-SHA512-TAI — RFC 9381 Verifiable Random Function** (suite_string `0x03`) over `std.crypto.ecc.Edwards25519` + SHA-512 — a VRF is the public-key analogue of a keyed hash: only the secret-key holder can compute `(pi, beta) = prove(sk, alpha)`, but anyone with the public key can `verify(pk, alpha, pi)` that `beta = proofToHash(pi)` is the unique deterministic output for `(pk, alpha)`, without learning the key or being able to bias/predict `beta`. Full clean-room transcription of RFC 9381 §5.1–§5.5: `prove` (Gamma = x·H, deterministic RFC 8032 nonce `k`, challenge `c` over the FIVE points Y‖H‖Gamma‖k·B‖k·H, `s = k + c·x`), `verify` (U = s·B − c·Y, V = s·H − c·Gamma via `mulDoubleBasePublic`, `c' == c`), `proofToHash`, and the §5.4 auxiliaries `encodeToCurve` (try-and-increment hash-to-curve + cofactor clear), `nonceGeneration`, `decodeProof` (rejects `s ≥ q`), `validateKey` (low-order/identity reject). Byte-exact against **all three RFC 9381 Appendix B.3 official vectors** (Examples 16/17/18 — `x`, `H`, `k_string`, `k`, `pi`, `beta` all pinned) plus tamper-reject tests (Gamma/c/s bitflip, wrong alpha, non-canonical `s`, small-order/identity PK → fail-closed `InvalidProof`/`InvalidPublicKey`). Pure orchestration over `std` — no new curve/field math. Consumers: blockchain leader election / committee sortition, DNSSEC NSEC5 authenticated denial of existence. ELL2 (`0x04`) + P-256 + RSA-FDH-VRF ciphersuites out of scope | any | — |
| `fss` | **Function Secret Sharing — 2-party single-point Distributed Point Function** (DPF; Boyle–Gilboa–Ishai optimized tree, ACM CCS 2016) — secret-shares a point function `f_{α,β}(x)=β if x==α else 0` into two `O(λ·n)` keys with `Eval(0,k0,x)+Eval(1,k1,x)==f_{α,β}(x)` in `Z_{2^{8L}}` while each key hides `(α,β)`; the primitive under Prio/Poplar private analytics, Riposte, and 2-server PIR. **Phase-1 COMPLETE:** the SHA-256 length-doubling PRG `G` + seed→group `convert`, the `Z2k(L)` output group, the `Cw`/`Key` types + byte codec, `evalAll`, the `firstMismatch` full-domain checker, and the whole deterministic harness are REAL; the Fable-irreducible core — the per-level **correction-word construction** (`genWithSeeds`, BGI16 Fig.1) + matching traversal (`eval`) — is implemented (`gate.core_implemented = true`). Anti-self-consistency anchor = byte-exact KAT vectors from an INDEPENDENT Python re-derivation (same construction + PRG), so a self-consistent-but-nonstandard core cannot pass. Full-domain exhaustive reconstruction, byte-exact KAT vs those vectors, the stronger CW-perturbation positive control, and the deterministic broken-share controls (`brokenAllBeta`/`brokenAllZero`) all execute, Debug + ReleaseFast. DCF/comparison functions, general FSS, fixed-key-AES PRG, and 2-server PIR out of Phase 1 | any | — |
| `bfv` | **BFV leveled homomorphic encryption over `Z_q[X]/(X^N+1)`, in RNS (Fan–Vercauteren, ePrint 2012/144; SEAL design) — complete leveled scheme: arithmetic backbone + keyGen/encrypt/decrypt + multiply/relinearize to depth.** Fully Homomorphic Encryption — compute on ciphertexts (`Dec(f(Enc(x)))=f(x)`); BFV is the cleanest EXACT-integer scheme and adds the ciphertext MULTIPLY the additively-homomorphic `paillier` lacks (first homomorphic-evaluation scheme in the repo — `std` ships only lattice KEM/sig). **REAL + byte-exact-KAT'd today (the FHE backbone):** word-size modular arithmetic mod an NTT prime (`modarith` — `mulMod`/`powMod`/`invMod`, deterministic Miller-Rabin, `primitive2NthRoot`), the **negacyclic NTT** (`ntt` — Cooley-Tukey forward / Gentleman-Sande inverse, O(N log N) multiply mod `X^N+1`, with an independent schoolbook cross-check), **RNS/CRT** over an NTT-prime chain (`rns` — exact reconstruct + base conversion), the RLWE ring `R_q` (`ring` — `RnsPoly` add/NTT/pointwise-multiply), plaintext `R_t` encodings + `addRef`/`mulRef` references (`encode`), and parameter/NTT-friendly-prime validation (`params`). The scheme layer (`Bfv(P)`) ships real types + the non-noise-sensitive `add`/`sub`, with both cores now REAL on their honestly-separated flags: **Part 2 (Opus)** `scheme_core_implemented` — `keyGen` (`pk=(−(a·s+e),a)`, ternary `s`) / `encrypt` (`Δ·m+p0·u+e0`, `p1·u+e1`) / `decrypt` (`⌊t/q·(c0+c1·s)⌉ mod t`, exact CRT reconstruct + round-half-up rescale); and **Part 3 (genuine Fable)** `fable_core_implemented` — `mul` (EXACT-integer tensor over `Z[X]/(X^N+1)` + `⌊t/q·…⌉` rescale — a mod-`q` ring tensor would decrypt to garbage), `genRelinKey`/`relinearize` (base-`2^8` gadget key-switch for `s²`), `noiseBudget`. Anchors: byte-exact NTT/RNS KATs vs an independent Python re-derivation + a deterministic noiseless-ciphertext decrypt KAT (pins the Δ-rescale) + enc/dec round-trip & homomorphic-add end-to-end (tiny + N=1024); and the mul+relin / multiply-DEPTH (`a·b·c`) end-to-end anchors over random AND all-`(t−1)` boundary plaintexts on `params.test_mul`, whose worst-case noise ledger makes depth-2 correctness a guarantee for every seed (not a lucky-seed pass). **Teeth:** three deliberately-broken positive controls — a cyclic-vs-negacyclic discriminator, a wrong-scale "encryptor", and a corrupted-relin-key catch — all PASS, Debug + ReleaseFast. Bootstrapping / CKKS / Galois key-rotation / CRT-slot batching / security-grade params = deferred increments; **no security level claimed** | any | — |
| `groth16` | **Groth16 zk-SNARK PROVER over BN254 (Groth 2016, "On the Size of Pairing-based Non-interactive Arguments") — Parts 1-2 COMPLETE: full prover, verifier-anchored.** The counterpart to the sibling `bn254` module's Groth16 VERIFIER: given an R1CS circuit + proving key (CRS) + satisfying witness, produce the 3-element proof `(πA∈G1, πB∈G2, πC∈G1)` that `bn254.groth16Verify` accepts. **Part 1 — the mechanical/math layer:** `Fr` helpers over the sibling `bn254` scalar field (`field`); dense `Fr[x]` polynomial arithmetic + exact division by the vanishing polynomial `Z(x)=x^n−1` (`poly`); the radix-2 evaluation domain — `n`-th roots of unity `ω=5^{(r−1)/n}` from BN254's 2-adicity-28 scalar field, vanishing polynomial (`domain`); the radix-2 **NTT / inverse-NTT over `Fr`** + FFT polynomial multiplication (`fft`); naive multi-scalar multiplication in `G1`/`G2` (`msm`); the rank-1 constraint system + witness satisfaction (`r1cs`); and **R1CS→QAP interpolation + the divisibility oracle** (`qap`) — `A·B−C` is divisible by `Z` **iff** the witness satisfies the R1CS. Sub-anchors: NTT round-trip (`intt(ntt(v))==v`), `mulViaFFT==schoolbook`, MSM `==naive`, QAP-divisibility `==` R1CS-satisfaction, and a deliberately-broken "prover" (`brokenProof`) that `bn254.groth16Verify` REJECTS (positive control). **Part 2 — the prover core (`gate.prover_core_implemented=true`):** `setup` (toy, INSECURE trusted setup — samples the CRS from explicit toxic-waste `{τ,α,β,γ,δ}`, a real deployment runs an MPC ceremony) builds the Groth16 proving/verifying keys (per-wire `uᵢ(τ)/vᵢ(τ)/wᵢ(τ)` bases, public `ic=…/γ`, private `l_query=…/δ`, `H`-query `τʲ·Z(τ)/δ`); `prove` computes the QAP quotient `H=(A·B−C)/Z` and assembles `πA=α+L+rδ`, `πB=β+R+sδ`, `πC=Σ_{priv}wᵢL_i+ΣHⱼh_query+s·πA+r·πB_g1−rsδ` via MSM. **End-to-end anchor: `prove(setup(…))→bn254.groth16Verify==true`** on a non-trivial circuit (4 constraints, 3 public inputs — `out1=x²,out2=y²,out3=(x+y)²`), with tamper (negated πA/πC), wrong-public-input, and non-satisfying-witness all REJECTED. **Tier call: Opus, not Fable** — the `bn254` verifier is a COMPLETE deterministic anchor (any prover error → verify returns `false`; no self-consistent-but-wrong failure mode a Fable core has). Debug + ReleaseFast, green. snarkjs `.zkey`/witness ingestion (byte-exact external anchor), Pippenger MSM, circuit/gadget DSL, PLONK/Halo2, recursion = deferred increments | any | bn254 |
| `tfhe` | **TFHE/FHEW programmable gate bootstrapping — unbounded-depth FHE via blind rotation** (Chillotti–Gama–Georgieva–Izabachène, J. Cryptology 2020, ePrint 2016/870; Ducas–Micciancio FHEW, EUROCRYPT 2015) — the counterpart to the *leveled* sibling `bfv`: instead of a bounded multiply budget, **every gate ends in a bootstrap** that homomorphically re-decodes the message through a programmable LUT and emits a FRESH low-noise ciphertext, so an arbitrarily deep circuit stays correct. Self-contained over the **power-of-two torus `Z_{2^32}`** (`k=1` RLWE): all arithmetic is exact wrapping-`u32`, so the negacyclic ring multiply is a plain `O(N²)` schoolbook convolution that is **byte-exact by construction — no NTT prime, no FFT precision bound** (it IS its own correctness oracle). **REAL + tested (mechanical backbone):** the negacyclic ring (`poly`), the signed/balanced gadget decomposition (`gadget`), torus (de)coding + `q→2N` modulus switch (`torus`), LWE/GLWE/GGSW keygen/encrypt/decrypt, bootstrap-key + key-switch-key generation, sample extraction, LWE→LWE key switching, and the **noiseless cleartext blind-rotation oracle `clearBootstrap`** that pins LUT construction + mod-switch + rotation indexing without the core. **The Fable core (`gate.fable_core_implemented=true` — four functions under one flag):** `externalProduct` (GGSW⊠GLWE gadget product), `cmux` (`d0 + C⊠(d1−d0)`), `blindRotate` (the `n`-CMux accumulator rotating by `−b̃+Σ ãᵢsᵢ mod 2N`), and `bootstrap` (mod-switch → blind-rotate → sample-extract → key-switch). **Sign convention resolved:** this scaffold puts the gadget term `+μ·wᵢ` in the GGSW rows (mask in block A, body in block B), so block A's phase is `eᵢ−μ·wᵢ·s` and the `−μ·s·a` piece emerges from the rows themselves — plain **positive** accumulation `Σ_r d_r·rows[r]` is correct, no negation anywhere (independently re-derived + fault-injection-confirmed: negating it goes RED). **No external byte-exact KAT** (TFHE-rs / OpenFHE-binfhe / concrete all differ in encoding + params) — **verified by property + positive-control + unlimited-depth chain:** three deliberately-broken positive controls (sign-dropped sample extraction, a dropped gadget level exceeding the error bound, wrong-sign rotation exponent) each demonstrably mis-decode while the real primitive is right; the end-to-end anchors run a programmable gate (identity/NOT + a 2-input homomorphic AND), a **16-deep bootstrap chain asserting the bit survives every refresh** (noise is RESET, not grown), a **corrupted-bootstrap-key positive control** (randomised bsk ⇒ mismatches>0), and an **output-noise-budget assertion `< Δ/4`**, Debug + ReleaseFast. **Tier note: Fable-lite** — the pre-validated `clearBootstrap` oracle + exact schoolbook ring + structurally-constraining signatures collapse most of the classic sign/off-by-one design space, but a `params.zig` **failure-probability** noise ledger (no deterministic worst-case oracle bounds the CMux-chain noise — the Fable trigger) keeps residual weight in the core. **Toy/test parameters only — NO security level claimed;** `k=1`, no wire byte-codecs, and circuit/multi-value bootstrapping are deferred increments | any | — |

### Serialization / OS / agent

| Module | What it does | Platform | Deps |
|---|---|---|---|
| `tar` | ustar/GNU tar reader+writer (preserves uid/gid/mtime) + gzip | any (packer: linux) | — |
| `linkheader` | Web Linking (RFC 8288) `Link` header build + parse (rel/title/type), `pagination` (first/prev/next/last), `find(rel)` — zero-alloc | any | — |
| `cookies` | HTTP cookies (RFC 6265) — `Cookie` request parser (`parse`/`find`) **and** `Set-Cookie` builder (`SetCookie` w/ Path/Domain/Max-Age/Expires/**Secure/HttpOnly/SameSite**, injection-guarded, SameSite=None⇒Secure) + `get`/`set` http helpers | any | http |
| `blobmsg` | OpenWRT ubus client + blob/blobmsg wire codec | **linux** (codec itself: any) | — |
| `mcp` | Model Context Protocol server (JSON-RPC 2.0) — tools + resources + prompts, app-state ctx | any | — |
| `mcp-http` | MCP **Streamable HTTP** transport (2025-06-18) — `POST /mcp` → JSON-RPC response (`application/json` **or live SSE** with tool-progress streaming) / 202, as a `router` middleware over a `mcp.Server`. Optional **sessions** (`Mcp-Session-Id` + `GET /mcp` server→client SSE stream with `Last-Event-ID` resumable replay + `DELETE` teardown); built-in **Origin** (DNS-rebinding) guard, size cap, Lock seam | any | router, http, mcp |
| `uci` | OpenWRT UCI config parser + serializer + typed model (stable round-trip) | any | — |
| `argsafe` | Allowlist validators + a typed argv builder — neutralizes argument/flag injection into an exec `argv` | any | — |
| `procrun` | Subprocess runner: reap-race-tolerant wait, deadlock-free capped stdio capture, timeout, streaming + cancel | any | argsafe |
| `pollworker` | Single-owner `poll(2)` loop + a lock-free fork/exec job table for offloading blocking work off the loop thread | **linux** | — |
| `ipcbus` | Same-host unix-socket control plane — request/reply server + a capped in-memory scratch key→bytes bus | **linux** | framing |
| `sandbox` | Process self-hardening for an internet-facing server — `no_new_privs` + order-safe **privilege drop** (verified) + `setrlimit`/no-core-dumps + **Landlock** fs allow-list (ABI-negotiated) + a **seccomp-bpf** syscall allow-list (arch-guarded, configurable deny action) | **linux** | — |
| `framing` | Length-prefixed stream framing (`writeFrame`/`readFrame`) + a generic JSON tagged-union envelope codec | any | — |
| `csvstream` | Streaming RFC 4180 CSV reader that preserves byte offsets, bounded memory regardless of file size | any | — |
| `csvsafe` | OWASP CSV formula-injection guard (`=`/`+`/`-`/`@` cell leads) | any | — |
| `json5` | Single-pass JSON5→JSON preprocessor (comments, unquoted keys, trailing commas, single-quoted strings) | any | — |
| `cbor` | **CBOR (RFC 8949) codec** — decode/encode all 8 major types, definite + indefinite length, a `canonical` deterministic-encoding option (§4.2.1 bytewise map-key sort); untrusted-input hardened (depth cap, no pre-alloc on a declared length). Plus a minimal **COSE (RFC 9052)** layer: `COSE_Key` (EC2/OKP) parse/build and `COSE_Sign1` parse/build + `Sig_structure` bytes — the keystone under WebAuthn/FIDO2 and general COSE | any | — |
| `zipstream` | Streaming ZIP archive reader — walk the central directory once, stream decompressed member bytes on demand | any | — |
| `encoding` | Legacy single-byte code page ↔ UTF-8 transcoding (5 European code pages: windows-125x, ISO-8859-1/2/15) | any | — |
| `datefmt` | Civil calendar + token-based date/time parse/format + calendar arithmetic, correct before 1970 | any | — |
| `tz` | IANA time-zone offset lookup — zone name → UTC offset/DST at a given instant (600 zones + POSIX-TZ footer) | any | datefmt |
| `numparse` | Locale-aware grouped-number parsing (thousands/decimal separators) into an exact `decimal.Decimal` | any | decimal |
| `diagnostics` | LSP-style structured validation-finding collector — severity, dot-path, position, code, suggestion | any | — |

## Roadmap / not yet built

Research-verdicted **DON'T-BUILD-YET** — no consumer demands them today; see each
module's `SPEC.md` (once built) or the git history for the full reasoning and the
"when greenlit" path for each:

- **`testkit`** (shared test harness) — deferred; the honest remaining scope (a
  `runWire` HTTP-wire test wrapper + `expectStatus` family + fake-clocks) needs a
  `build.zig` test-only-dep mechanism with no precedent in this repo, plus a 19-module
  refactor to pay off.
- **`Reconcilable(T)`** (generic desired-vs-actual reconciler) — no second consumer
  exists yet; extract a small `RollbackTimer` (arm/confirm/overdue) first, once one
  appears.
- **`kv` on-disk MVCC / transactions / ordered scans** — shipped as the separate
  `kvtree` module (copy-on-write B-tree; `kv` v0 stays for point-store consumers
  like `jobqueue`). The transactional core (`commit`/`recover`/reclaim gate) is
  implemented and its property + crash-sweep tests run for real. Remaining work =
  grow the crash sweep into a full randomized VOPR (fuzzed op/crash schedules
  chained across epochs) and add merge/overflow-page/freelist-chaining (backlog).

## Non-goals — deliberately not built here

Durable scope decisions (candidate audit, 2026-07-09): capabilities this collection will
not own, and what to reach for instead.

### Adopt instead of building

| Capability | Adopt instead | Why not a module |
|---|---|---|
| Hardened/read-only SQLite | `vrischmann/zig-sqlite` or `karlseguin/zqlite.zig`, wrapped consumer-side | The enforcement (`authorizer`/`PRAGMA query_only`/`open_v2(READONLY)`) is raw C-API — breaks the pure-Zig/no-libc invariant |
| Kafka | bind `librdkafka` | External C client, no pure-Zig alternative |
| gRPC | build over our HTTP/2 + adopt `Arwalk/zig-protobuf` | Needs an external protobuf codec; no trustworthy pure-Zig gRPC exists |
| Regex | `mnemnion/mvzr` (no captures) or `zig-utils/zig-regex` (captures) | Two mature pure-Zig libs already exist |
| PostgreSQL (wire v3) | `karlseguin/pg.zig` | Mature MIT lib, pooling + TLS |
| MySQL/MariaDB | `speed2exe/myzql` | Only viable option |
| SMTP | `karlseguin/smtp_client.zig` | Mature MIT lib (TLS-1.2 caveat) |
| WebSocket | `karlseguin/websocket.zig` | Mature MIT lib, both roles |
| protobuf | `Arwalk/zig-protobuf` | De-facto pure-Zig implementation |
| TOML | `mattyhall/tomlz` | Mature MIT config parser |
| Templates | `jetzig/zmpl` (comptime-typed) / `batiati/mustache-zig` (logic-less) / `gremlin-labs/vibe-jinja` (runtime `.jinja` corpora only, pilot) | Zig comptime makes a runtime engine mostly unnecessary |
| Structured logging | `karlseguin/log.zig` | Cleanest "just use it" |
| S3 | `lobo/aws-sdk-for-zig` | SigV4 built in |
| Redis/Valkey | `kristoff-it/zig-okredis` (partial/alpha) | Best available design |
| YAML (flat/nested config) | `kubkon/zig-yaml` / `pwbh/ymlz` (both partial — no anchors/tags) | Fine for config; not 1.2-complete (see full YAML below) |

### Won't build

- **`exprcalc`** — app-specific spreadsheet/rules engine, not reused cross-project, and needs external regex.
- **`unaccent`** — fully dependent on external `uucode` tables; not included.
- **`roquery`** — C-level SQLite hardening (authorizer/query_only enforcement); lives consumer-side over adopted zig-sqlite.
- **`taskqueue`** — folded into `jobqueue`.
- **`chunkframe`** — too small to be a module; a documented ~20-LOC pattern instead.
- **YAML 1.2 (full spec)** — no adoptable complete pure-Zig implementation; the partial libs above cover config use.
- **Jinja** — Zig comptime templating covers dev-authored use; `vibe-jinja` is a consumer-side pilot option for runtime `.jinja` corpora only.
- **IMAP** — no mature pure-Zig lib; stays unbuilt.
- **HTTP/3 (QUIC)** — not researched, stays dropped.
